# 跨端待办 · TODO

结构：Root（产品）→ 模块 → 功能 → 功能点。功能点为原子操作粒度（新增 / 删除 / 编辑各为一条），每点独立标注状态、就地变更，流转 ⏳ → 🚧 → ✅。需求源 README.md「产品定义」；工程规范 AGENTS.md；视觉基准 DESIGN.md。

状态图例：✅ 已实现 ｜ 🚧 部分实现（附现状说明） ｜ ⏳ 未开始 ｜ （设想）= 产品定义之外的候选项，实现前需求确认。

版本范围：v1.0 = 树上全部功能点均在本版本内完成，唯二例外为标注「远期储备」的 9.6 手表端与 13.5 多工作区。「Windows 先行，macOS / Linux 随后，移动端最后」是 v1 内的实施端序，不是版本切分；同步上线初期免费公测，订阅收费随 v1 内多端就绪开启（12.6）。

```text
跨端待办
├── 1 任务管理
│   ├── 1.1 统一创建界面（产品定义：新建待办在一个界面完成全部设置）
│   │   ├── ✅ 基础字段：标题 + 可选截止日、提醒时刻（统一创建容器 + 可展开详情区；字段块 TodoFields.vue 与行内编辑共用）
│   │   ├── ✅ 时间段字段：可设起止时刻，不只截止日（起止一对为一个字段，结束早于/等于开始被拒；7.8 时间块规划复用）
│   │   ├── ✅ 周期设置：重复规则 每日 / 每周 / 每月 / 每年 / 自定义间隔 / 指定星期几 / 月末 / 工作日（联动 1.3）（周期进 `TodoFields.vue` 声明式字段表新增 `recurrence` control 分支、委托子组件 `RecurrenceField.vue` 直接读写 `draft.recurrence`，`App.vue`/`TodoItem.vue` 零改动；渐进式显示按频率收放子控件；`until`/`count` 做成单一 radio group 三态使二者并存结构不可达——收敛引擎与 08 `.ics` 导出（只写 UNTIL）不一致的入口；月内位置单选使契约 month_day/on_last_day 并存态界面不可达；阻塞校验间隔 1–999、次数 ≥1 对齐契约 `RecurrenceRule::validate`；`isSameDraft` 对 recurrence 键改结构比较；`recurrence.ts` 新增 `recurrenceSummary` 内联回显（`recurrenceLabel` 仍供 08 列表 pill 不动）；闰月/永不复现位置以探针 `nextOccurrence(probe,anchor,anchor)===null` 显琥珀告警、无截止日锚点同步告警，均非阻塞，引擎仅 Tauri 可用、浏览器降级不显；契约层 `RecurrenceRule::validate` 补拒 `until`+`count` 并存，堵死界面/引擎/`.ics` 三方不一致的最后入口）
│   │   ├── ⏳ 标签设置：多标签可选（联动 3.2）
│   │   ├── ⏳ 象限设置：重要 × 紧急标记（联动 4.1）
│   │   └── ⏳ 清单设置：归属清单可选（联动 13.2）
│   ├── 1.2 任务操作（任务为统一实体，单次 / 周期通用）
│   │   ├── ✅ 完成
│   │   ├── ✅ 取消完成
│   │   ├── ✅ 删除
│   │   ├── ✅ 编辑：列表行内可改标题 / 截止日 / 提醒 / 备注 / 开始日期 / 时间段 / 估时（点标题进编辑，显式保存；详情区按有无可选值决定开态）
│   │   ├── ✅ 撤销：Ctrl+Z 撤销完成 / 删除 / 编辑（Pinia 操作栈，栈深 50 条、溢出弃最旧；编辑态内让位给输入框原生文本撤销；新建与复制不入栈但清重做栈，自动归档/顺延与入站同步两者都不动）
│   │   ├── ✅ 重做：撤销后可重做（Ctrl+Y 或 Ctrl+Shift+Z；撤销与重做均以正向补偿 op 进同步日志）
│   │   ├── 🚧 归档：已完成任务定期归档、归档页可查（列表保持轻快）（契约 archivedAt + 自动归档满 7 天 + 主列表下方「已归档」区可查可还原；独立归档页待导航壳落地后搬迁）
│   │   ├── ✅ 逾期顺延：逾期未完成自动滚到今天（可开关）（设置面板「任务」开关，默认关；只改 open 且逾期任务的截止日，无截止日不动；启动 + 本地 00:00 定时器 + visibilitychange 触发，跳过编辑中的行）
│   │   ├── ✅ 备注/描述字段（创建与编辑共用，上限取自契约 MAX_NOTES_CHARS）
│   │   ├── 🚧 复制任务：一键复制（含子任务 / 标签 / 象限 / 清单设置）（复制按全量字段表执行，子任务/标签/象限/清单的值原样带走，依赖不带；这些实体的界面归 1.4、模块 3、模块 4、模块 13）
│   │   ├── ✅ 附件与链接：任务挂本地文件引用与 URL，可挂 / 移 / 打开（编辑态第三兄弟区块 `AttachmentList.vue`：并列「链接」文本录入与「文件」系统对话框两入口；store 新增 `editAttachments`/`sameAttachments` 通道，整份 `attachments` 数组走 `applyChange`+`recordHistory`，一次离散动作＝一条撤销条目；打开经原生 opener 命令 `open_url`/`open_path`（自由函数、返回可捕获失败），文件对话框 `pick_attachment_file`（dialog 插件）；界面层挡 `javascript:`/`data:` scheme，打开目标只作单一参数交 OS 不拼 shell；文件丢失/被移动 `await` 捕获失败经唯一 live region 降级不崩溃；非 Tauri 运行时链接降级 `window.open(_,_,"noopener,noreferrer")`、文件播报降级不禁用；附件云端上行属订阅段，不做）
│   │   ├── 🚧 开始日期：未到开始日收进「将来」视图不打扰（GTD 推迟）（字段、判定函数与列表行「N 天后开始」徽标已落；分组/移出/计数/日切归 7.1）
│   │   ├── ✅ 任务依赖：前置任务完成后才解锁可见 / 提醒（编辑态第四兄弟区块 `DependencyList.vue`：搜索框＋结果列表选前置，封顶 8＋溢出提示，候选剔除自身/已选/成环项；锁定任务**显示不隐藏**，折叠行加中性 `Lock` 徽标——单前置「待前置完成」、多前置「前置 n/m」——前置全完成即消失＝解锁；环检测落 domain `crates/domain/src/dependency.rs` 纯图算法（`introduces_cycle`/`transitive_prerequisites`/`transitive_dependents`/`has_cycle`/`dependency_lock`，`cargo test` 覆盖），store 侧 `wouldCreateDependencyCycle` 预剔除候选、`editDependsOn` 提交拒绝复用等价 TS 逻辑；`editDependsOn`/`sameDependsOn` 通道整份 `dependsOn` 走 `applyChange`+`recordHistory`，一次动作一条撤销；`dependencyLock` getter 跨图派生锁定态，无法解析前置（已删除/未同步）算「已满足」不阻塞（编排者裁决）；提醒 gating：锁定任务不排提醒（`refreshReminder`/`rescheduleAll` 接 `isLocked`），前置完成/删除经 `refreshDependents` 重排、解锁时刻已过按现状即时补发；多级依赖可视化不做（范围））
│   │   └── 🚧 估时字段：预估用时，与实际专注时长对比（联动 11.2 / 7.8）（字段已落，仅接受大于 0 的整数分钟；与实际专注时长的对比归 11.2）
│   ├── 1.3 周期属性（仅作用于带周期的任务）
│   │   ├── ✅ 实例生成：完成或到期后自动生成下一次实例（完成路径 store `toggle` 生成新实例，与完成合为一次可撤销单元；到期路径 `advanceRecurrences` 在日切/回前台把逾期周期任务顺延到下一次发生日；下次日期取自 Rust 命令 `next_occurrence`，锚点＝当前实例截止日、`count` 逐次递减、`until`/`count` 用尽即不再生成；companion 日期字段按位移随之平移；重启/同步重放不重复生成；引擎仅 Tauri 运行时可用，浏览器降级为不生成）
│   │   ├── 🚧 高级规则：每周一三五 / 每月最后一天 / 法定工作日（domain 周期引擎已落：`recurrence::next_occurrence` 解释每日 / 每周（含星期集合）/ 每月（含月末）/ 每年 / 自定义间隔 / 工作日并算出下次日期，与 ICS 的 RRULE 映射同源；工作日已按国务院放假通知感知调休；界面入口归 1.1「周期设置」、实例生成归本节「实例生成」）
│   │   ├── 🚧 农历周期：农历生日 / 节日重复（国内市场刚需）（引擎已落：`RecurrenceCalendar::Lunar` 的每月 / 每年按农历计算，闰月按「有则落、无则跳」，`crates/domain/src/lunar.rs` 由朔望月与太阳黄经算得、有效年份 1929–2100（1929 年中国启用东八区标准时，此前按北京地方平时推算）、越界显式报错；界面入口归 1.1「周期设置」）
│   │   ├── 🚧 节假日感知：法定节假日调休数据源（「法定工作日」规则的底座）（`crates/domain/src/holidays.rs` 内置 2024–2026 国务院放假通知，每年一条、加一年只改表；表外年份回退周一至周五，且由 `recurrence::Occurrence::assumed_monday_to_friday` 随答案报出、配合 `holidays::coverage()` 提示数据覆盖到哪年；自动远程更新未做，随版本更新）
│   │   ├── ✅ 打卡视图：每日 / 每周周期任务以习惯打卡形态展示（打卡 / 补卡；原独立习惯模块并入此处）（主列表行变体，不新增列表/区/页；完成圈即打卡＝把该次排期日记进契约新增 `check_ins`（SQLite v4 列、随同步整组下发如 tagIds）并把本行推进到下一次排期、不留兄弟已完成行——其余频率仍走 08 `completeRecurring`；补卡只写历史、不建行、不改锚点；streak 与近期补卡候选由 domain `habit::habit_progress` 从打卡历史对排期表纯函数派生、不落计数器，引擎新增反向区间枚举 `recurrence::occurrences_between` 与 `next_occurrence` 同源、覆盖农历/工作日调休；行内 emerald streak pill（`Flame`＋`streakLabel`，断连归 0 不显）＋最多 3 枚近期补卡按钮；打卡/补卡各为一条可撤销 `change`；引擎仅 Tauri 运行时可用，浏览器降级为无 pill/补卡）
│   │   └── ✅ 周期标识与下次到期展示：主列表行 `TodoItem.vue` 非编辑态显示「`Repeat` 图标＋频率文字」中性灰 pill（`recurrenceLabel`），完成态褪色；下次到期即当前 open 实例的既有到期徽标，不新增第二个日期
│   └── 1.4 子任务（任务内 checklist）
│       ├── ✅ 新增子任务：编辑态 `SubtaskList.vue` 底部输入回车/按钮提交，空白无操作，达上限 200（契约 `MAX_LIST_ENTRIES`）经唯一 live region 播报后不生效
│       ├── ✅ 完成子任务：行首原生 `<input type=checkbox> accent-sky-500` 即时落库；与父任务完成解耦（勾满不自动完成父任务）
│       ├── ✅ 取消完成子任务：同上复选框切回未完成
│       ├── ✅ 编辑子任务：行内标题输入，blur/Enter 提交一次（一条可撤销记录），码点截断至 `MAX_TITLE_CHARS`
│       ├── ✅ 删除子任务：行尾 ghost `Trash2`，删后焦点落相邻行
│       ├── ✅ 子任务排序：上/下移按钮（键盘/读屏/触屏主通道，播报新位次）+ `sm:` 原生 HTML5 拖拽手柄；顺序即 `subtasks` 数组顺序
│       └── ✅ 进度显示：非编辑态行只读徽标 `n/m`（`ListChecks`，中性 slate，仅有子任务时显示），随勾选实时更新（store 侧新增 `editSubtasks` 通道：整份 `subtasks` 数组走 `applyChange`+`recordHistory`，一次离散动作＝一条撤销条目；复制/周期携带子任务由 05/08 承担，无契约改动）
├── 2 提醒通知
│   ├── 2.1 桌面即时提醒
│   │   ├── ✅ 到点系统通知（桌面，应用运行期）
│   │   └── ✅ 通知权限静默请求，拒绝后优雅降级
│   ├── 2.2 后台与周期调度
│   │   ├── ✅ 后台调度：应用未运行时提醒不丢（Rust 侧调度）（提醒时序下沉 Rust：`reminder_scheduler.rs` 后台线程每 30s 轮询本地 SQLite，纯规则 `todo_domain::reminder::due_reminders` 判定到点/逾期，并从数据重导 gating——完成·归档·依赖锁定（`dependency_lock`）不排；`reminder_deliveries` 表按 (todo_id, reminder_at) 记已发，防同进程重发、重启后据此重建调度表不遗漏；逾期（超 60s 宽限）以「【逾期】」标题标注补发，通知走既有 `tauri-plugin-notification` 的 Rust 端直发，不经 webview capability，故 capability 无需新增；前端 `src/lib/notifications.ts` 的 `scheduleReminder`/`cancel*` 降级为惰性——Tauri 桌面走 Rust、浏览器 dev/移动端本就无后台调度，杜绝两套并行重复发；`instant_unix_seconds` 复用 ics 的 instant 解析，与日历导出同一读法；`cargo test` 覆盖判定/标注/投递持久化，驻托盘触发与重启补发属真实桌面运行时验证边界。移动端本地通知仍归 2.3）
│   │   └── ✅ 周期提醒：随重复规则持续触发（由 2.2 的轮询架构天然满足，本任务只验证不新增运行时代码）：周期实例（任务管理/08 `buildNextInstance` 换新 id、任务管理/09 打卡与 `advanceOverdueRecurring` 原地推进）都把 `reminder_at` 经 `shiftInstant`（`toISOString`，`.000Z`）位移到下一次发生，于是每个实例落一个新的 `(todo_id, reminder_at)` 投递键——下一轮 `due_reminders` 现算即发新实例、旧键已投递不重发，连续两次实例（含跨日）各提醒一次；`crates/domain/src/reminder.rs` 新增 2 项单测锁定该链（新 id 路径 + 同 id 原地推进路径，均用 `.000Z` 格式跨层验 `instant_unix_seconds` 读法）。「入站同步重排提醒」（编排者并入项：远端完成前置/改提醒时间/周期推进）同样由轮询满足——`applyRemoteUpsert`/`applyRemoteDelete` 落 SQLite 后下一轮（≤30s）重导 gating，无需重启或本地再操作；对应判定已由既有单测 `a_locked_task_holds_its_reminder_until_the_prerequisite_is_done`（完成前置→解锁重排）、`a_reminder_edited_to_a_new_time_is_raised_again`（改提醒时间→再发）与本次周期单测覆盖。轮询 30s 延迟对任务级提醒可接受（60s 逾期宽限已把窗口内到点归为准点），未改
│   ├── 2.3 移动端提醒
│   │   └── ⏳ 移动端本地通知（iOS / Android）
│   ├── 2.4 通知交互
│   │   ├── 🚧 通知上稍后提醒（snooze）：降级为应用内快捷条（Windows 通知动作按钮能力不足）（snooze 机制＝把 `reminderAt` 改到未来时刻，下一轮 2.2 轮询按新 `(todo_id, reminder_at)` 键重发、旧键已投递不重发，故零契约/零 Rust 运行时代码，前端 `todoStore.update(id,{reminderAt})` 即可；主交付＝应用内 snooze 快捷条 `src/components/ReminderSnoozeBar.vue`，挂主窗顶部列表上方，对「刚到点、仍待处理」的提醒（与 `due_reminders` gating 同口径：open ∧ 未归档 ∧ 未依赖锁定 ∧ reminderAt≤now，前端纯派生自 store、不新增 command）逐条给稍后/完成/跳过，`v-if=isDesktop` 门控、反馈走既有单一 live region（`page-alert.ts` 增补 `reminder` 源）；**通知气泡动作按钮：经核实 `tauri-plugin-notification` 2.3.3 桌面路径 builder 不透传 action、`show()` 弃句柄无点击回收，Windows 需绕开插件用 winrt+COM＝新增依赖+大量原生工作，按规格定为「能力确认后再接的渐进增强」，本任务不实现**；「点通知唤前台」同因不接，快捷条对任意方式打开应用都可见故非阻塞。移动端本地通知与其快捷条接入仍归 2.3）
│   │   ├── ✅ 自定义稍后时长：snooze 快捷选项可配（5 分钟 / 1 小时 / 今晚 / 明天）（设置面板新增「提醒」fieldset，四档各一开关复选框、默认全开、不做自定义数值输入；键 `reminder.snooze.{5m,1h,tonight,tomorrow}` 走既有 `saveBooleanPreference`/`loadBooleanPreference`（含浏览器回退）、按 `rolloverOverdue` 同一模式落 `useSettingsStore`；今晚固定 20:00、明天固定 09:00 常量实现（将来复用「默认提醒时间」外观与设置/02、「晨间摘要时间」提醒通知/06 的接线点），「今晚」仅 20:00>now 时呈现；快捷条只渲染启用且解析为未来时刻的档，全部取消勾选给内联提示不硬拦）
│   │   └── ✅ 到期重催：到期未完成持续提醒（可设开关）（以 `dueDate` 逾期为锚的递减退避重催：`crates/domain/src/renag.rs` 纯规则 `due_renags` 算「当前该发的那一次」——退避 +1h/+3h/+7h/+15h 后每 24h 封顶、不硬停，gating 镜像 01 的 `due_reminders`（完成/归档/无坏 dueDate/依赖锁定），锚点 `(days_from_civil(dueDate)+1)*86400 - zoneOffset` 精确等价前端 `isOverdue`；每次重催是合成键 `renag:<unix>`，与 01 真实 `reminderAt`（ISO）前缀命名空间不碰撞，**共读同一 `reminder_deliveries` 表、各看各键**、沿用「每键一次」故无契约/无新表；`latest_renag_offset` 只返回最近一次故跨机器停机重启单轮只补 1 条、每日封顶跨多日成立。开关 `reminder.renag` 默认关：设置面板「提醒」fieldset 顶部单复选框（`useSettingsStore.renagOverdue` 走既有 `saveBooleanPreference`）；**新增可复用「Rust 读提醒偏好」通道** `src-tauri/src/reminder_prefs.rs`——沿用 01 `close_to_tray_enabled` 既有的 `app.store("settings.json")` 直读（前后端共享同一进程内 `Arc<Store>`，前端写即调度线程下轮见），供 05 勿扰/06 晨间/07 时区复用；重催在 `reminder_scheduler.rs::poll_once` 骑 01 同一 30s 轮询与 `show()` 发送路径（不绕过轮询即时发，故 05 勿扰只需发送前加门即可叠加），通知文案 `逾期未完成 · <标题>` / `已逾期 N 天，完成后不再提醒。` 与 01 首次/`【逾期】`补发相区分、不加列表徽标；时区 offset 域函数已备参数、调度器暂传 0（UTC 读日界＝东向准点偏晚、西向早催的已知限制交 07 供真实 offset），退避/去重/停催/跨多日/offset 由 11 项 cargo test 覆盖，原生发送链属真实桌面验证边界）
│   └── 2.5 提醒策略
│       ├── ✅ 勿扰时段：静默窗口内提醒暂存，结束后补推（通知门控行为，引擎为主界面露一角：`crates/domain/src/quiet.rs` 纯规则 `plan_quiet_delivery(due_instants, window, now, offset)`——窗内全压（不发不记投递＝天然暂存于 `reminder_deliveries` 差集）、窗外把「自身时刻落在窗内」的暂存批 ≥2 折一条摘要/恰 1 归个别发、其余个别发，`window=None`（关或起==止或不可解析）→全个别发 fail-open 守「零丢失」；`QuietWindow` 起含止不含、跨午夜取并集，恰 22:00 静默、恰 08:00 放行。`reminder_scheduler.rs::poll_once` 收拢 due reminders + due renags（含重催，同一道门）经 `plan` 决定发法，`DueItem` 统一二者 instant/投递键/文案，摘要文案 `勿扰时段已结束` / `勿扰期间有 N 条提醒待查看。`（不带 `【逾期】`）；每条恰记一次投递故「不重复」由既有去重表天然成立。三个偏好 `reminder.quiet.enabled/start/end` 落 `settings.json`，Rust 经 04 的 `reminder_prefs.rs` 新增 `quiet_hours_enabled`/`quiet_hours_window`（默认关、`22:00`/`08:00`）读取，关时计划＝全个别发＝01/04 原行为不变、无新表无契约无 bindings 漂移。设置面板「提醒」fieldset 内勿扰开关（默认关）+ 渐进展开一对 `type="time"` 起止（`CONTROL_CLASS` 内联）+ 人话说明句现算（跨午夜「次日」/同日/空窗/静默中反馈，`aria-describedby` 关联），无新增设计令牌。时区 offset 域函数已备参数、调度器暂传 0（同 04，交 07）；05/06 早晨双通知边界保持分离（编排者裁决，已登记 06）。判定/边界/合并/去重/fail-open 由 domain 15 项 cargo test 覆盖，勿扰门/补推属真实桌面验证边界）
│       ├── ✅ 晨间摘要：每日一条汇总通知（今日到期 / 逾期 / 我的一天）（定时聚合通知，引擎为主界面露一角：`crates/domain/src/summary.rs` 纯规则 `due_morning_summary(todos, delivered, prefs, now, offset)`——开关关/未到时刻/当天已发/空态（三段计数全 0）任一即 `None`，否则算 body（非零段以 ` · ` 连接、`今日到期 N 条 · 逾期 M 条。`，零段略去、末尾句号）。每日去重用 `reminder_deliveries` 合成键 `summary:<本地YYYY-MM-DD>`（todo_id 定值 `morning-summary`），前缀命名空间与 01 真实 `reminderAt`（ISO）、04 `renag:<unix>` 均不碰撞、共读同表各看各键；键系本地日故跨日自动重置、停机跨多日重启只补当日一条不 burst。计数口径＝**列表徽标**（`src/lib/dueDate.ts`）：open ∧ 未归档 ∧ `dueDate` 本地日＝今日→今日到期、＜今日→逾期，**不排除依赖锁定**（有意区别于 renag 更严 gating）；「我的一天」段源（视图与统计/06）未落地，`my_day_count()->None` 缺省不显、留接缝（落地时填 Some 即自动并入 body，无界面改动）。`reminder_scheduler.rs::poll_once` 在 due reminders/renags 批之后单独处理摘要：`due_morning_summary` 每轮从 `db.list()` 现算计数，经**独立单项** `plan_quiet_delivery([scheduled_unix])` 骑 05 同一勿扰门——窗内暂存不发不记（下轮按补发时刻现算，故顺延后计数最新）、窗外发一条独立 toast；单项 plan 永不 `summarize` 故摘要与 05 补推**分离不合并**（05/06 边界定案）。两偏好 `reminder.summary.enabled`（默认关）/ `reminder.summary.time`（默认 `09:00`＝全应用「早晨」锚点、与 03 `TOMORROW_HOUR=9` 重合零漂移，跨接「明天」snooze 留待 外观与设置/02）落 `settings.json`，Rust 经 04 的 `reminder_prefs.rs` 新增 `morning_summary_prefs` 读；`crate::quiet::parse_hm` 提为 `pub(crate)` 供摘要复用同一 `"HH:mm"` 解析避免漂移。设置面板「提醒」fieldset 勿扰块之后新增摘要开关（默认关）+ 渐进展开 `type="time"` 推送时刻（`CONTROL_CLASS` 内联、`aria-describedby`）+ 说明句现算（含「时刻落勿扰窗内→顺延到勿扰结束」提示），无新增设计令牌、无契约/无 bindings 漂移。时区 offset 调度器暂传 0（同 04/05，交 07）。触发/去重/跨日/空态/勿扰门控/计数口径由 domain 10 项 cargo test 覆盖，原生定时发送属真实桌面验证边界）
│       ├── ⏳ 多级提前提醒：同一任务多个提醒点（截止前 1 天 / 1 小时 / 15 分钟，联动 1.1）
│       └── 🚧 时区策略：跨时区提醒正确性（本地时刻 / 固定时刻可选）（真实设备偏移收敛 + 补推逾期文案已落地，本地时刻 floating 与选择开关随 08 归并：`reminder_scheduler.rs` 以 `chrono::Local::now().offset().local_minus_utc()` 每轮现读设备实时 UTC 偏移（复用依赖树已锁定的 chrono 0.4.45，Cargo.lock 仅增一行引用、零新增 crate），替换 04/05/06 四处 `ZONE_OFFSET_SECONDS=0`，经 `poll_once(now, offset)` 喂进域函数已备的 `zone_offset_seconds`——从 OS 现读而非前端缓存，驻托盘跨时区/夏令时不用陈值，东八区等主市场逾期锚点/今日计数/勿扰窗/晨间时刻不再一律按 UTC 偏晚。补推「【逾期】」文案口径统一：勿扰窗内暂存后单发的逾期提醒去掉「【逾期】」前缀（与 ≥2 摘要路径一致，勿扰晚发有意为之非漏发），复用域 `local_minute_of_day`/`QuietWindow::contains` 判「自身时刻是否落窗内」。固定时刻（absolute）语义即现状——`reminderAt` 经 `src/lib/datetime.ts::toInstant` 存绝对 UTC、调度器按该绝对瞬间触发。**本地时刻（floating）语义与「本地/固定」全局单选未做**：绝对 UTC 已丢失原始时区信息，正确 floating 必须改 `reminderAt` 存储形态、与 08 的 `reminder_at` 单值→多提醒点契约迁移强耦合，按全局规则 5 停下交编排者归并 08；选择开关与 floating 不可分故一并延后。domain 偏移判定（renag 东/西锚点、quiet minute-of-day、summary 东/UTC 计数与推送时刻）+ 补推文案由 cargo test 覆盖，真实改系统时区走查属桌面运行时验证边界）
├── 3 标签
│   ├── 3.1 标签管理
│   │   ├── ⏳ 新增标签
│   │   ├── ⏳ 重命名标签
│   │   ├── ⏳ 删除标签
│   │   └── ⏳ 标签颜色：沿用 DESIGN.md 语义色公式
│   ├── 3.2 任务贴标签
│   │   ├── ⏳ 添加标签：创建（1.1）与编辑时可选，支持多标签
│   │   └── ⏳ 移除标签
│   └── 3.3 筛选与统计
│       ├── ⏳ 按标签筛选：作为智能列表（7.7）预置项实现，不再单建一套筛选
│       └── ⏳ 标签维度统计（联动 7 视图与统计）
├── 4 四象限
│   ├── 4.1 象限属性
│   │   ├── ⏳ 标记象限：任务标记 重要 × 紧急 二维（创建时可设，见 1.1）
│   │   └── ⏳ 默认推断：按截止日临近度预填紧急维度（产品定义）
│   └── 4.2 象限视图与操作
│       ├── ⏳ 四象限总览：2×2 网格视图
│       ├── ⏳ 快速设置象限：列表/详情内
│       ├── ⏳ 象限内排序
│       └── ⏳ 跨象限拖拽（拖动即改 重要 × 紧急 标记）
├── 5 账号与登录
│   ├── 5.1 账号基础
│   │   ├── ⏳ 注册
│   │   ├── ⏳ 登录
│   │   ├── ⏳ 登出
│   │   ├── ⏳ 密码找回：忘记密码邮件重置
│   │   ├── ⏳ 修改邮箱 / 密码
│   │   └── ⏳ 注销账号：含云端数据抹除（商店审核与 GDPR 双重要求）
│   ├── 5.2 离线与设备
│   │   ├── ✅ 未登录离线完整可用（现状架构即离线优先）
│   │   └── 🚧 设备标识：deviceId 已用于同步，尚未与账号绑定
│   └── 5.3 数据归属与鉴权
│       ├── ⏳ 数据归属：登录后本地数据归属账号
│       ├── ⏳ 多设备管理
│       ├── ⏳ 服务端鉴权：/v1 接口携带凭证（现状 /v1/sync 无鉴权；账号与订阅体系的前置）
│       └── ⏳ 反滥用限流：注册验证、API 限流、同步频控
├── 6 数据与同步
│   ├── 6.1 本地持久化
│   │   ├── ✅ Store 键值持久化（桌面；浏览器开发回退 localStorage）
│   │   ├── 🚧 SQLite 落地：桌面端已落地（rusqlite + command，repository 收口，非 Tauri 回退 localStorage），移动端待随移动形态验证
│   │   └── 🚧 数据迁移：启动时把 SQLite 之外的待办写入重放进库（旧数据 + 降级期间的新增/修改/删除），逐条容错、坏行隔离；桌面端已落地，移动端待随移动形态验证
│   ├── 6.2 自动同步
│   │   ├── ✅ 操作式同步：桌面 ↔ Axum，op-based，冲突服务端优先、字段级合并
│   │   ├── ✅ 同步状态展示：状态 / 上次同步时间 / 错误
│   │   ├── ✅ 服务端持久化：操作日志落 SQLite（rusqlite 单文件自托管，WAL + synchronous=FULL），重启后日志、游标与去重集完整
│   │   ├── ✅ 服务端备份容灾：定时快照（VACUUM INTO 走同一条连接，默认每小时、保留 72 份）、过期清理与 `todo-server restore <快照>` 恢复子命令；patch JSON 出库放宽未知字段，二进制回滚后旧行不再被静默跳过
│   │   ├── ✅ 监控告警：`/v1/health` 报可用性、错误率、同步时延与坏行清点（120 秒滚动窗口），阈值命中即写告警日志、恢复自动解除，unhealthy 回 503 供探活；同步请求移出异步 worker（`spawn_blocking`）
│   │   ├── ⏳ 移动端同步接入
│   │   ├── ⏳ 登录绑定：同步随账号而非仅 deviceId
│   │   └── ✅ 契约扩展：周期规则、标签、象限、清单、子任务、备注、开始日期、时间段、估时、附件链接、依赖、手动排序进 contracts（含校验方法）并重生成 bindings；SQLite 表随 schema v2 迁移、同步 patch 覆盖全字段（各字段的业务界面见对应模块）
│   └── 6.3 日历集成
│       ├── ✅ ICS 导出：设置面板「日历」一键导出 .ics（RFC 5545 折行/转义/UTC，周期规则映射为 RRULE；农历与「每 N 个工作日」标准表达不了，导出为单次事件并在提示条说明条数；周期结束日 `UNTIL` 按导出设备本地时区换算，UTC 以西不再少展开一次），文件写入下载目录、同名不覆盖；桌面端已落地，移动端随移动形态验证
│       └── ⏳ ICS 订阅：系统日历（Outlook / Apple / Google）订阅任务日历
├── 7 视图与统计
│   ├── 7.1 列表视图
│   │   ├── ✅ 分组：进行中 / 已完成
│   │   ├── ✅ 截止日徽标：今天 / 明天 / 逾期（三档语义色）
│   │   ├── ⏳ 日期分组：进行中按 今天 / 明天 / 本周 / 以后 / 逾期 分组
│   │   └── ⏳ 手动拖拽排序：顺序持久化（排序字段进契约，联动 6.2）
│   ├── 7.2 任务统计
│   │   └── ⏳ 当日 / 本周 / 本月任务数与完成率：数据卡置于成就页（7.4）顶部，不单独设页；月历表与移动卡片直读同一数据源
│   ├── 7.3 月历 / 周历视图
│   │   ├── ⏳ 月视图：按日任务分布与完成标记（主应用内实现，桌面月历表复用）
│   │   └── ⏳ 周视图：一周七列任务分布（与 7.8 时间块规划互补）
│   ├── 7.4 回顾与激励（收拢为单一「成就」页，导航不碎片化）
│   │   ├── ⏳ 周 / 月完成情况回顾页（闭合「执行 → 回顾」）
│   │   ├── ⏳ 完成热力图
│   │   ├── ⏳ 连续完成天数（streak）
│   │   ├── ⏳ 成就周报：每周自动生成完成摘要（模板版免费；AI 自然语言版为订阅档，见 14.3）
│   │   ├── ⏳ 成就分享卡片：周报 / 热力图 / streak 生成图片分享到社交
│   │   ├── ⏳ 拖延洞察：顺延 / 改期次数统计，识别卡壳任务并提示拆解（可接 14.1）
│   │   └── ⏳ 成就徽章：里程碑徽章（首任务 / 百任务 / 30 天 streak，联动分享卡片）
│   ├── 7.5 我的一天（唯一「今日」入口）
│   │   ├── ⏳ 今日清单：每天从全部任务中挑选今日执行项
│   │   └── ⏳ 联动收敛：晨间摘要（2.5）推它、时间块（7.8）排它、月历表 / 今日卡片（8.3）显它
│   ├── 7.6 全文搜索
│   │   ├── ⏳ 按标题 / 备注全局搜索
│   │   └── ⏳ 快捷键直达搜索
│   ├── 7.7 智能列表
│   │   ├── ⏳ 组合筛选：标签 / 象限 / 清单 / 日期条件组合
│   │   ├── ⏳ 保存为自定义视图（侧边栏直达）
│   │   ├── ⏳ 编辑智能列表
│   │   └── ⏳ 删除智能列表
│   └── 7.8 时间块规划（timeboxing）
│       ├── ⏳ 今日时间轴视图
│       ├── ⏳ 任务拖入时间块安排
│       └── ⏳ 联动：专注计时（11）与日历集成（6.3）
├── 8 桌面端形态
│   ├── 8.1 主窗口
│   │   ├── ⏳ 主窗口导航：「任务 / 视图」两段切换（视图结构，README）
│   │   └── ⏳ 键盘全操作：列表导航 / 快速完成 / 改期 / 打标签快捷键体系（自定义统一在 10.2 快捷键设置）
│   ├── 8.2 托盘与唤起
│   │   ├── ✅ 系统托盘：菜单、今日数量、双击唤起
│   │   ├── ✅ 关闭隐藏到托盘（设置可关）
│   │   ├── ✅ 全局快捷键唤起（CommandOrControl+Shift+A）
│   │   └── ⏳ 迷你捕捉窗：全局快捷键弹出独立小输入窗秒记（内置一键贴入剪贴板文本，点击时才读取、不监听），不唤起主窗
│   ├── 8.3 嵌入式月历表（今日卡片为其前身，不再独立演进）
│   │   ├── ✅ 今日卡片窗口（现状）：置顶、DPI 感知定位、与主窗 event 双向同步
│   │   ├── ⏳ 月视图（任务分布 + 完成标记 + 月份切换）
│   │   ├── ⏳ 统计区（当日 / 周 / 月任务数与完成率）
│   │   ├── ⏳ 任务速览，点击唤起主窗定位任务
│   │   ├── ⏳ 卡片上勾选完成（不唤起主窗）
│   │   └── ⏳ 卡片上快速新增
│   ├── 8.4 常驻与单实例
│   │   ├── ⏳ 嵌入桌面层级
│   │   ├── ⏳ 开机自启
│   │   └── ⏳ 单实例（依赖 tauri-plugin-single-instance 就绪）
│   └── 8.5 Windows 深度集成（首发差异化）
│       ├── ⏳ 任务栏角标：今日剩余任务数
│       ├── ⏳ 跳转列表：快速新增 / 查看今日 / 唤起月历表
│       ├── ⏳ Win11 小组件面板接入（需评估 Tauri 可行性）
│       └── ⏳ 视觉融合：Mica / Acrylic、深色标题栏、跟随系统主题色
├── 9 移动端形态
│   ├── 9.1 应用框架
│   │   ├── ⏳ 主界面框架：「任务 / 视图 / 我的」底部导航（视图结构，README）
│   │   └── ⏳ 「我的」页：账号与登录态、设置、关于
│   ├── 9.2 构建与适配
│   │   ├── ⏳ 移动构建初始化：tauri ios/android init（需对应工具链机器）
│   │   └── ⏳ 移动端适配：触控目标 ≥44px、safe-area、手势（DESIGN.md 基准）
│   ├── 9.3 常驻桌面卡片
│   │   ├── ⏳ 当月统计（iOS WidgetKit / Android AppWidget）
│   │   ├── ⏳ 当日 / 周 / 月任务速览
│   │   ├── ⏳ 卡片点击直达应用内对应视图（deep link）
│   │   └── ⏳ 卡片数据随任务变更自动刷新
│   ├── 9.4 移动交互
│   │   ├── ⏳ 滑动手势：列表项左右滑 完成 / 删除 / 改期
│   │   ├── ⏳ 语音输入：系统语音识别快速记待办
│   │   └── ⏳ 系统分享接入：share sheet 目标，任意应用分享文本 / 链接存为任务
│   ├── 9.5 系统入口与适配
│   │   ├── ⏳ iOS 锁屏小组件
│   │   ├── ⏳ Android 通知栏常驻快添
│   │   └── ⏳ 平板分栏布局（iPad / Android 平板，列表 + 详情）
│   └── 9.6 手表端（远期储备，不进近期版本）
│       └── ⏳ Apple Watch / Wear OS 速览与勾选
├── 10 外观与设置
│   ├── 10.1 主题
│   │   ├── ✅ 主题切换：浅色 / 深色 / 跟随系统
│   │   └── ✅ 启动恢复主题偏好
│   ├── 10.2 设置面板
│   │   ├── ✅ 设置面板：主题、关闭隐藏、今日卡片开关
│   │   ├── 🚧 快捷键设置：全局与应用内快捷键统一面板（与 8.1 键盘体系共用；现状注册机制支持替换全局键，入口待建）
│   │   ├── ⏳ 默认提醒时间
│   │   └── ⏳ 周起始日
│   ├── 10.3 反馈动效
│   │   ├── ⏳ 完成动画（DESIGN.md 动效基准延伸）
│   │   └── ⏳ 完成音效（可关）
│   └── 10.4 无障碍
│       ├── ⏳ 字号缩放：应用内字号档位
│       ├── ⏳ 高对比模式（呼应 DESIGN.md 已知按钮对比度不达 AA 问题）
│       └── ⏳ 减弱动效：跟随系统 prefers-reduced-motion（联动 10.3）
├── 11 专注计时
│   ├── 11.1 番茄钟
│   │   ├── ⏳ 开始专注：绑定任务计时
│   │   ├── ⏳ 暂停 / 继续专注
│   │   ├── ⏳ 结束 / 放弃本次专注
│   │   ├── ⏳ 时长设置：专注 / 休息时长
│   │   └── ⏳ 专注悬浮条：置顶迷你条显示当前任务与倒计时（不占屏）
│   ├── 11.2 专注统计
│   │   └── ⏳ 专注时长记录并计入统计（联动 7.2）
│   ├── 11.3 专注音景（订阅线候选）
│   │   ├── ⏳ 白噪音 / 环境音播放
│   │   └── ⏳ 音景选择与音量
│   └── 11.4 禅模式
│       └── ⏳ 全屏单任务视图：当前任务 + 计时（配合专注音景）
├── 12 发布工程（Windows 先行，macOS / Linux 随后，移动端最后）
│   ├── 12.1 新手引导
│   │   ├── ⏳ 首启引导流程
│   │   ├── ⏳ 示例任务数据
│   │   └── ⏳ 空状态优化
│   ├── 12.2 自动更新
│   │   └── ⏳ 应用内检查更新与升级（tauri-plugin-updater，直发渠道；Store 渠道由商店接管）
│   ├── 12.3 质量遥测
│   │   ├── ⏳ 崩溃上报
│   │   └── ⏳ 匿名使用统计（设置可关）
│   ├── 12.4 多语言
│   │   ├── ⏳ i18n 框架落地
│   │   └── ⏳ 中英双语
│   ├── 12.5 分发（双渠道）
│   │   ├── ⏳ Windows 安装包与签名（首发范围）
│   │   ├── ⏳ Microsoft Store 上架
│   │   ├── ⏳ 官网 / GitHub 直发
│   │   └── ⏳ Linux 打包矩阵：deb / rpm / AppImage / Flatpak（桌面补全阶段）
│   ├── 12.6 订阅体系（免费 + 订阅增值；上线初期同步免费公测，收费随 v1 内多端就绪开启）
│   │   ├── ⏳ 功能分层：订阅含 多端同步（5/6）、AI 助手（14）、协作共享（13.4）、开放集成（15.1/15.2）；本地功能免费
│   │   ├── ⏳ 订阅购买与支付接入（双市场双通道，见 12.7）
│   │   ├── ⏳ 定价：轻订阅 ¥6–10/月 基准档（美元价对齐换算；AI 用量设配额控成本）
│   │   ├── ⏳ 订阅管理：价格页、恢复购买（换机）、退订、发票凭证
│   │   └── ⏳ 订阅状态绑定账号（联动 5）
│   ├── 12.7 双市场合规与支付
│   │   ├── ⏳ 国内：ICP 备案、国内云节点、微信 / 支付宝支付
│   │   ├── ⏳ 海外：GDPR 合规、Stripe / Paddle 支付、海外节点
│   │   └── ⏳ 服务端多区域部署与数据分区
│   └── 12.8 运营配套
│       ├── ⏳ 应用内反馈：bug 上报（自动附日志）+ 功能建议
│       ├── ⏳ What's New：更新后首启新功能引导
│       ├── ⏳ 帮助中心：内置帮助页与快捷键速查表
│       └── ⏳ 用户协议与隐私政策：应用内展示 + 首启同意流程（商店上架必需）
├── 13 清单
│   ├── 13.1 清单管理
│   │   ├── ⏳ 新增清单
│   │   ├── ⏳ 重命名清单
│   │   ├── ⏳ 删除清单
│   │   └── ⏳ 清单排序：侧边栏顺序调整
│   ├── 13.2 任务归属（单清单模型：一个任务属一个清单，多维分类交给标签）
│   │   ├── ⏳ 加入清单：创建（1.1）与编辑时可选
│   │   └── ⏳ 移动 / 移出清单
│   ├── 13.3 清单视图
│   │   └── ⏳ 侧边栏清单切换筛选
│   ├── 13.4 共享协作（依赖账号 5 与服务端，订阅线候选）
│   │   ├── ⏳ 邀请成员共享清单
│   │   ├── ⏳ 移除成员 / 退出共享
│   │   ├── ⏳ 任务指派给成员
│   │   └── ⏳ 共享变更同步与通知
│   └── 13.5 多工作区（远期储备，不进近期版本）
│       └── ⏳ 个人 / 工作数据隔离与切换
├── 14 AI 助手（订阅增值线，联动 12.6 功能分层）
│   ├── 14.1 AI 拆解子任务
│   │   ├── ⏳ 大任务自动生成子任务清单（落 1.4）
│   │   └── ⏳ 生成结果可调整后采纳
│   ├── 14.2 智能标签象限
│   │   └── ⏳ 创建时预填标签与象限建议，确认后生效（联动 1.1）
│   ├── 14.3 AI 周报
│   │   └── ⏳ 自然语言总结本周完成与下周建议（与 7.4 成就周报为同一功能的订阅档）
│   ├── 14.4 智能我的一天
│   │   └── ⏳ 按截止日 / 象限 / 历史习惯推荐今日清单（7.5 增强）
│   ├── 14.5 图片 OCR 转任务
│   │   └── ⏳ 拍照 / 截图识别文字转任务（白板、书页、聊天截图）
│   ├── 14.6 AI 自动排程
│   │   └── ⏳ 「我的一天」按估时 / 象限优先级自动排进时间块，变动自动重排（联动 1.2 估时、7.8）
│   └── 14.7 智能入箱建议
│       └── ⏳ 无日期 / 无清单任务定期建议归档、排期或拆解（GTD 收件箱整理）
└── 15 开放与集成（原模块 16 顺移；原模块 15 习惯打卡已并入 1.3 打卡视图与 7.4 激励体系）
    ├── 15.1 邮件转任务（订阅线候选，依赖服务端）
    │   ├── ⏳ 专属转发地址生成
    │   └── ⏳ 邮件转发自动建任务（标题 + 正文进备注）
    ├── 15.2 开放 API / Webhook（订阅线）
    │   ├── ⏳ REST API：任务读写
    │   ├── ⏳ Webhook：任务变更推送
    │   └── ⏳ API Token 管理
    ├── 15.3 URI 深链
    │   ├── ⏳ todo:// 协议注册
    │   ├── ⏳ 深链快添任务
    │   └── ⏳ 深链定位任务 / 视图（与 9.3 卡片直达同一机制）
    └── 15.4 网页只读分享
        ├── ⏳ 清单 / 任务生成只读网页链接（拉新入口）
        └── ⏳ 分享链接管理与失效（关闭分享）
```
