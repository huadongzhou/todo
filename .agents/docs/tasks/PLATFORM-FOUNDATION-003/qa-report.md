# QA 报告

## 结果摘要

已实现并静态验证通过；完整 Tauri 桌面运行时测试受本机动态链接环境阻断（与前两里程碑相同），待修复后复验运行时行为。

## 已实现并可静态验证

- [x] `Todo` 结构包含 `dueDate` / `reminderAt`，TS 同步类型一致。
- [x] `tauri-plugin-notification 2.3.3` 已加入依赖，`notification:default` 权限已授予。
- [x] 表单含"截止日"与"提醒时间"控件，可独立留空。
- [x] 列表项含截止日徽标，逾期红色、当日/明天强调色、其余中性色，不以颜色为唯一提示。
- [x] `rescheduleAll` 在启动时重调度未完成提醒。
- [x] 首次发通知静默请求权限；被拒绝写 `warn` 日志，不抛错。

## 待运行时复验（受环境限制）

- [ ] 提供 `reminderAt` 后到点触发系统通知（桌面）。
- [ ] 应用启动时对已过期的提醒立刻补发一次。
- [ ] 删除 / 取消待办可取消对应提醒。
- [ ] 浏览器无插件路径安全跳过。

## 未决问题

- 完整 Tauri 测试进程在本机启动前以 `STATUS_ENTRYPOINT_NOT_FOUND` 退出，与新增代码无关。
- 当前提醒调度基于前端 `setTimeout`，仅在应用运行时有效；如需要应用关闭期间的提醒，需后续叠加 Rust `NotificationBuilder::schedule` 后台调度。
