# 发布准备

## 当前状态

未准备发布。基础工程未实现生产数据、账号或发布签名，且 Rust crate 检查尚待复验。

## 后续前置条件

1. 在可访问 crates.io 或可用镜像的环境完成 `cargo check` 与当前主机 `npm run tauri:build`。
2. 由具备 Android SDK、macOS/Xcode 和签名权限的负责人初始化并构建移动端。
3. 进行干净设备上的新增、完成、恢复和删除验收。
4. 人工确认合并主分支及任一平台安装包发布。

## 回滚

本阶段没有数据迁移；发布后若有阻断问题，停止分发并恢复上一已知可用安装包。
