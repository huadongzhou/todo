# 落地方案 10：外观与设置

- 来源：TODO.md 模块 10（10.1 主题 ✅ / 10.2 设置面板 / 10.3 反馈动效 / 10.4 无障碍）
- 前置方案：08（键盘体系的快捷键表单源就绪后，快捷键设置面板才有内容可管）
- 端序阶段：Windows 首发（W4 尾）
- 母任务：未生成（建议名 `外观与设置`）

## 范围

- 10.2 快捷键设置统一面板（🚧 清账：全局 + 应用内一个面板）、默认提醒时间、周起始日。
- 10.3 完成动画、完成音效（可关）。
- 10.4 字号缩放、高对比模式、减弱动效（跟随系统 prefers-reduced-motion）。
- 边界：10.1 主题为 ✅ 回归项；帮助中心/速查表归方案 12（读同一份快捷键表）；周起始日的消费方在方案 07（周视图/周报）。

## 技术要点

- 快捷键面板：读写 08 定义的快捷键单源表；全局键改绑走现有注册机制（现状已支持替换），冲突检测（与系统/应用内已占用键）在保存时校验。
- 偏好落库：默认提醒时间、周起始日进设置存储（现有 Store 设置通道或随 06 并入 SQLite，设计阶段定一处，不双写）。
- 动效与无障碍：完成动画按 DESIGN.md 动效基准延伸；`prefers-reduced-motion` 生效时动画与音效自动降级；高对比模式作为主题变体实现，须一并修复 DESIGN.md 已记录的「按钮白字压 sky-500 不达 AA」问题（该遗留项在此清账）；字号档位用相对单位缩放（UnoCSS 根字号），检查不溢出。

## 拆分建议（母任务拆解清单参考）

| 序号 | 子任务（短名） | 级别建议 | 依赖 | 一句话验收 |
|------|--------------|---------|------|-----------|
| 01 | 快捷键设置面板 | 简化 | 无 | 全局+应用内快捷键一处可改，冲突被拦截，🚧 转 ✅ |
| 02 | 默认提醒时间与周起始日 | 简化 | 无 | 两偏好生效于新任务与周视图/周报 |
| 03 | 完成动效与音效 | 简化 | 无 | 动画/音效可关，reduced-motion 下自动禁用 |
| 04 | 字号缩放与高对比 | 简化 | 无 | 档位缩放不溢出；高对比主题过 AA（含按钮遗留问题） |

## 方案级验收标准（母任务整体验收依据）

- [ ] 快捷键在设置面板改绑后，8.1 键盘操作与 12.8 速查表同步反映（单源验证）。
- [ ] 周起始日改动后周视图与周报起点即时变化；默认提醒时间作用于新建任务。
- [ ] 系统开启减弱动效时应用零动画零音效；高对比模式全界面对比度达 WCAG AA（含既有按钮问题清账）。
- [ ] `pnpm run check` 与手动走查（浅/深/高对比 × 三档字号矩阵抽查）通过；TODO.md 模块 10 状态同步。
