# 提醒通知

- 级别：母任务
- 状态：完成
- 来源方案：.agents/plan/02-提醒通知.md

## 需求（整体）

- 目标与场景：提醒可靠性与体验全量落地——应用未运行提醒不丢（Rust 侧调度）、周期提醒、snooze/重催、勿扰/晨间摘要/多级提醒/时区策略。
- 范围（做 / 不做）：做 TODO.md 模块 2 除 2.3 外全部功能点（2.1 为 ✅ 回归项）。不做：2.3 移动端本地通知（随「移动端形态」波次执行，本母任务仅预留接口）。
- 外部前置：数据与同步/01、/03 完成；02 周期提醒另需 任务管理/06、/08 完成。
- 整体验收标准（逐条可检查）：
  - [ ] 应用驻托盘（主窗关闭）时提醒零丢失；完全退出后重启，逾期提醒补发并有标注。
  - [ ] snooze / 重催 / 勿扰 / 晨间摘要 / 多级提醒各自可开关、互不冲突（勿扰期内重催同样静默暂存）。
  - [ ] 跨时区场景（改系统时区）按所选策略正确触发，有手动走查记录。
  - [ ] `cargo test --workspace`、`pnpm run check` 通过；TODO.md 模块 2（除 2.3）状态同步。

## 拆解清单

| 序号 | 子任务文件 | 级别 | 依赖 | 一句话验收 |
|------|-----------|------|------|-----------|
| 01 | 01-Rust调度底座.md | 直达 | 无 | 驻托盘时到点提醒仍触发；重启后调度表重建 |
| 02 | 02-周期提醒接入.md | 直达 | 01 | 周期任务每次实例到点均提醒 |
| 03 | 03-snooze与自定义时长.md | 简化 | 01 | 稍后选项可配且到时再触发 |
| 04 | 04-到期重催.md | 简化 | 01 | 未完成持续重催、完成即停、开关生效 |
| 05 | 05-勿扰时段.md | 简化 | 01 | 窗口内静默暂存、结束补推 |
| 06 | 06-晨间摘要.md | 简化 | 01 | 每日定时一条汇总，今日到期/逾期/我的一天三段齐全 |
| 07 | 07-多级提醒与时区.md | 简化 | 01 | 本地/固定时刻语义正确；调度器读设备真实时区偏移（**编排者 2026-07-24 修正**：多提醒点拆到 08，07 只做时区） |
| 08 | 08-多级提醒.md | 简化 | 01 | 一任务多提醒点全触发；契约 reminder_at 单值迁移到列表无损（**编排者 2026-07-24 追加**：自 07 拆出，07 原称此契约已在 03 完成实不成立） |

## 集成验收

### 第 1 轮

范围＝8 个子任务「实现记录」的并集（01 调度底座 / 02 周期 / 03 snooze / 04 重催 / 05 勿扰 / 06 摘要 / 07 时区偏移 / 08 多提醒点契约迁移）；核对依据＝「需求（整体）」4 条整体验收标准逐条复测，重点查子任务拼接处。**无阻塞。** 逐条结论与拼接核查如下。

**整体验收标准 1（驻托盘零丢失 + 完全退出重启逾期补发并标注）——通过（判定逻辑），实时 toast 属真实桌面边界。** `reminder_scheduler.rs` 后台线程每 30s 轮询 SQLite、纯规则 `due_reminders` 判定，`reminder_deliveries` 表持久记已发、重启读回即重建调度表不遗漏不重发，逾期（超 60s 宽限）打「【逾期】」标题。**08 迁移后仍成立**：`todo_db.rs` schema v5 新增 `reminders` JSON 列，`SELECT_TODOS`/`UPSERT_TODO` 已切读写该列、不再读 `reminder_at`（无半读）；`list()`→`due_reminders` 遍历 `todo.reminders` 列表、投递键＝`(todo_id, reminder.at)`（存储瞬间 `at`，跨 floating/absolute 与跨时区稳定）；`migrate_legacy_reminders` 仅对 `reminders IS NULL` 旧行把 `reminder_at` 折成单条列表、幂等且清空后不回填（`a_legacy_single_reminder_is_folded...` 实测）。故重启读 SQLite reminders 列表重建 → 零丢失链未被迁移破坏。

**整体验收标准 2（snooze/重催/勿扰/晨间/多级各自可开关、互不冲突，勿扰期内重催静默暂存）——通过。** 各开关（`reminder.renag` / `reminder.quiet.enabled`+`start`/`end` / `reminder.summary.enabled`+`time` / snooze 四档 / `reminder.tz.absolute`）经**单一** `reminder_prefs.rs` 每轮现读——四/五处偏好共用 `read_bool`/`read_string`→`app.store("settings.json")`，无各写一套、无第二份偏好真相。`poll_once` 叠加顺序核查无互吞/重复：① reminders（带 floating/absolute 解释）+（开关开时）renags 合入**同一批** `items`，经**同一道** `plan_quiet_delivery` 勿扰门——窗内 plan 空则二者皆不发不记（**勿扰期内重催同样静默暂存 ✓**）、窗外 deliver 个别发 + summarize（≥2）折一条；② 晨间摘要在该批之后经**独立单项** `plan_quiet_delivery([scheduled_unix])` 骑同一勿扰门（`window`、`zone_offset` 与主批同源），单项 `held.len()≤1` 永不 `summarize`，故**受勿扰门控但绝不折进 05 补推**（06 §4 分离不合并 ✓）；关开关则该轮不进对应 pass、下一轮即停。投递键命名空间三分支互不碰撞：reminder＝ISO `at`、renag＝`renag:<unix>`、summary＝`summary:<本地日>`，共读同一 `reminder_deliveries` 表各看各键。

**整体验收标准 3（改系统时区按策略正确触发，有手动走查记录）——判定逻辑通过；偏移收敛彻底；真实改时区手动走查未做（真实桌面边界）。** 07 已把四处 offset 源**全部**统一为 `current_zone_offset_seconds()`（`chrono::Local` 每轮现读，非缓存）：`due_reminders`、`due_renags`、主 `plan_quiet_delivery`、`due_morning_summary` 及其独立 `plan_quiet_delivery`、逾期文案的 `local_minute_of_day` 均经 `poll_once(now, zone_offset)` 透传真实偏移。全仓 grep 确认**无残留** `ZONE_OFFSET_SECONDS=0` 或 offset=0 硬编码（04/05/06 曾传的 0 已被 07 全替换）。floating/absolute 由 `reminder.rs::reminder_trigger_unix(at,offset,absolute,current_offset)` 实现、08 迁移为每点存 `{at, offset}`、全局开关 `reminder.tz.absolute` 每轮现读。**边界如实评估**：「改系统时区触发」的实时响铃与「手动走查记录」需真实桌面 + 运行中 Tauri + 改 OS 时区，8 子任务一致以此为验证边界、判定逻辑靠 cargo test 覆盖（floating vs fixed、同区一致、DST、东/西锚点、minute-of-day 偏移）——此贯穿边界可接受（见末节）。

**整体验收标准 4（cargo test / pnpm check 通过；TODO 模块 2 除 2.3 状态同步）——通过。** 验证命令全绿（见末行）。TODO 模块 2 除 2.3（移动端本地通知，本母任务不做）外核对：2.1/2.2/2.5 全 ✅，2.4「自定义稍后时长」「到期重催」✅；两处非 ✅ 项均属**合理延后非缺口**——(a) 2.4「通知 snooze」🚧：经核实 `tauri-plugin-notification` 2.3.3 桌面路径不透传动作按钮、`show()` 弃句柄无点击回收，降级为应用内快捷条已满足「稍后到时再触发」需求，通知按钮为「能力确认后再接的渐进增强」，🚧 标注合理；(b) 相对提醒「截止前 N」延后（08 用户 2026-07-25 裁决）：`dueDate` 纯日期缺时刻锚 + 带载荷枚举需复核服务端未知字段路径，登记为未来便利层另立子任务，属跨模块合理延后。

**拼接处核查（重点，均无阻塞）：**

- 契约单一事实源无半迁移：08 契约迁移彻底——`renag.rs`/`summary.rs` 核查确认**只读 `due_date`**（renag 锚 `(days_from_civil(dueDate)+1)*86400-offset`、summary 按 `dueDate` 本地日计数），`reminders` 仅出现在其测试构造体，逻辑零改、非半迁移；`reminder.rs::due_reminders` 已全量遍历列表读 `reminder.at`；前端 grep 确认无残留 `reminderAt` 字段读（仅 `todo-repository.ts::foldLegacyReminder` 迁移代码与生成物注释）。契约 `Reminder` 为普通 struct（无 `#[serde(flatten)]`、无载荷枚举），serde 派生 FIELDS 表含 `at`/`offset`，故服务端 `patch_from_json` 未知字段路径**无需改**（08 评审已核）。
- 偏好读取一致：04/05/06/07/08 复用同一 `reminder_prefs.rs`，未各开 store。
- 已知悉/建议级（均已在子任务记录、编排者已裁，集成层无新增）：① 08 评审建议——同任务两个**完全相同**提醒点单轮多弹一条（`reminder_deliveries` 跨轮去重、单轮内不去重），自伤级、编排者裁定可选收口；② 06 已登记限制——跨午夜勿扰窗**傍晚段**摘要被压过日界丢一条（退化配置、无重发/错时发）；③ 03 收尾建议（转 外观与设置/04）——snooze 最后一条读屏播报的 Vue flush 批处理边界，需真实 NVDA 环境实测。三者均不影响 4 条整体验收标准。

**需真实桌面 / 跨模块的合理延后（如实汇报，非集成缺陷）：** (1) 驻托盘零丢失、完全退出重启逾期补发的**实时 toast**（标准 1）与改系统时区触发的**手动走查记录**（标准 3）——贯穿 8 子任务的真实桌面验证边界，本非交互环境无法构建/运行桌面应用或改 OS 时区，判定/持久化/标注/偏移/去重逻辑已由 296 项 cargo test 覆盖；集成层未发现「必须真实桌面才能暴露」的拼接缺陷（发送路径、勿扰门、偏移透传、投递键、迁移在源码与测试层均自洽）。建议在装 Windows 桌面（及一台可改时区/装 NVDA）的环境走一次端到端：驻托盘到点、完全退出重启补发【逾期】、改系统时区后 floating/absolute 各按语义响铃、勿扰窗结束补推合并、晨间摘要定时发——补齐标准 1/3 的原生走查记录。(2) 移动端本地通知（2.3）随移动端波次；相对提醒「截止前 N」随未来便利层——均母任务范围外的合理延后。

验证：cargo test --workspace 通过（296 passed / 0 failed：cross_platform_todo_lib 34 + todo_contracts 14 + todo_domain 190 + todo_server 58）；cargo check --workspace 通过（无 warning/error）；pnpm run check 通过（59 文件格式、63 文件无 lint/类型错误）；pnpm run types:generate 通过、`git status src/bindings crates/contracts` 干净零漂移。走查未跑 `tauri:dev`（真实桌面边界，避免污染 production appdata），未向 production appdata 写数据。


### 集成验收裁定（编排者，2026-07-25）

集成评审 0 阻塞，拼接处质量核实到位：契约 `reminder_at`→`reminders` 迁移无半迁移残留（renag/summary 只读 `due_date`、todo_db 读写全切 reminders 列）、时区偏移四处源统一为 07 的 `current_zone_offset_seconds`（无残留 offset=0）、投递键三命名空间（`at`/`renag:`/`summary:`）互不碰撞、`reminder_prefs` 偏好单一事实源被 04/05/06/07/08 一致复用、各功能叠加在同一 `poll_once` 顺序正确无互吞。整体验收标准逐条：

- 标准 2（各功能开关互不冲突，勿扰期重催静默暂存、摘要受门控不折进补推）、标准 4（cargo test 296 / pnpm check 通过、TODO 模块 2 除 2.3 同步）：**完全满足**。
- 标准 1（驻托盘零丢失、重启逾期补发标注）、标准 3（改系统时区按策略触发、走查记录在案）：**判定逻辑完全满足并由 296 项 cargo test 覆盖；实时 toast + 改 OS 时区的手动走查记录本非交互环境无法产出**——这是贯穿 8 个子任务的真实桌面验证边界，非缺陷。集成层未发现「必须真实桌面才能暴露」的拼接缺陷。**遗留一项待办：在 Windows 桌面（可改时区、装 NVDA）走一次端到端补齐标准 1/3 的原生走查记录**（与 数据与同步/08 的 ICS 真实日历导入、外观与设置/04 的读屏批量实测同属真机验证清单）。

合理延后（母任务范围外或用户裁决，非缺口）：2.4 snooze 通知按钮（桌面插件能力不足、已降级应用内快捷条，需求满足）、相对提醒「截止前 N」（08 用户拍板延后为未来便利层，需先解决 dueDate 时刻锚）、移动端 2.3（移动端波次）。

3 条建议级（08 完全相同提醒点单轮多弹、06 傍晚段摘要退化、03 snooze 末条读屏 flush）均已在子任务裁决在案，集成层无新增。

判定：**本母任务无集成级阻塞缺陷，8 个子任务拼接质量高，4 条整体验收标准的判定逻辑全部满足；标准 1/3 的实时原生走查记录挂真机验证清单，不阻塞收尾。** 状态置「完成」。
