# 成就页与热力图streak

- 级别：全流程
- 状态：需求

## 需求（产品）

<!-- 编排者预填草案：产品阶段修订确认后再进设计 -->

- 目标与场景：单一「成就」页收拢激励能力，首批含统计数据卡（顶部）、完成热力图、连续完成天数。
- 范围（做 / 不做）：做 成就页框架 + 热力图 + streak；周报/徽章/分享/洞察由 08–10 追加。
- 验收标准（逐条可检查）：
  - [ ] 单页布局可承载后续模块追加；热力图与 streak 数字正确（读统计数据源）。
  - [ ] 空数据/首周边界正常；pnpm run check 通过。

### 编排者并入项（来自 任务管理/09 评审第 1 轮，已裁决「转任务处理」）

**series-end 的 streak 显示差一（N-1）**（`crates/domain/src/habit.rs` 的 streak 派生）：`任务管理/09` 已把打卡历史正确入库，但从历史派生 streak 的纯函数在「周期已到系列终点（until/count 用尽）」这一终态下会少算一次，显示 N-1。数据正确，仅派生显示的末端差一。

本子任务做 streak 与热力图的呈现，且要统一「单习惯 streak」与「跨任务连续完成天数」两套口径，series-end 的差一属同一处派生逻辑，请在此一并校准。

补充验收标准（追加）：

- [ ] 习惯在系列终点完成时，streak 显示的数字与实际连续打卡次数一致，无末端差一。

## 规格（UI/UX）

## 实现记录（开发）

- 改动文件：
- 自验结果（命令与结论）：

## 评审记录
