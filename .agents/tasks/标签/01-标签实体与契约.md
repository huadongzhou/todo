# 标签实体与契约

- 级别：直达
- 状态：开发

## 需求（产品）

- 目标与场景：Tag 实体（id/名称/颜色）与任务多对多关系进 contracts 与库表。
- 范围（做 / 不做）：做 实体、级联语义（删标签=从任务摘除，不删任务）、bindings 重生成；不做 界面。
- 验收标准（逐条可检查）：
  - [ ] types:generate 同步且调用方编译通过；删除级联语义有 cargo test。

### 编排者并入项（来自 视觉设计稿/02 评审第 1 轮，已裁决「转任务处理」）

**标签分类调色板未定义为 DESIGN.md 令牌**：视觉设计稿 02（主窗列表）里标签分类色点用了既有语义 hue 的 -500 档（emerald/amber/sky）作占位，但「标签分类调色板」（用户给标签选色用的一组可选色 + 默认分配规则）尚未在 DESIGN.md 定义为令牌。本子任务落地标签实体时，请定一组分类色 token 并按全局规则 3 经编排者确认后同步进 DESIGN.md；视觉设计稿 02/07 的标签占位色届时对齐该 token。

补充验收标准：

- [ ] 标签分类调色板作为设计令牌定义（可选色集 + 默认分配），同步进 DESIGN.md，标签色不孤靠颜色（伴文字/形状可辨）。

## 规格（UI/UX）

## 实现记录（开发）

- 改动文件：
- 自验结果（命令与结论）：

## 评审记录
