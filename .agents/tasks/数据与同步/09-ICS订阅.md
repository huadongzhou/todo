# ICS订阅

- 级别：直达
- 状态：开发

## 需求（产品）

- 目标与场景：服务端提供只读 ICS feed，外部日历可订阅并随任务变更刷新。
- 范围（做 / 不做）：做 feed 端点（签名 URL 或凭证保护）、变更刷新；不做 双向写回。
- 验收标准（逐条可检查）：
  - [ ] 外部日历订阅 feed 可见任务，任务变更后刷新可见。
  - [ ] feed URL 不可枚举、可吊销；无凭证访问被拒。

## 规格（UI/UX）

## 实现记录（开发）

- 改动文件：
- 自验结果（命令与结论）：

## 评审记录
