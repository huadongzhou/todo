# 打卡视图

- 级别：简化
- 状态：完成

## 需求（产品）

- 目标与场景：每日/每周周期任务以习惯打卡形态展示（原独立习惯模块并入此处）。
- 范围（做 / 不做）：做 打卡/补卡交互与连续记录；不做 热力图统计（视图与统计 7.4）。
- 验收标准（逐条可检查）：
  - [ ] 打卡/补卡可用，连续记录（streak 数据）正确入库。
  - [ ] 打卡即完成当日实例，与 08 实例生成一致。

## 规格（UI/UX）

本节接着 `任务管理/08`（周期标识 pill、`completeRecurring` 完成即生成下一实例、`recurrenceLabel`）与 `05`（归档区「区非页」的可搬迁做法、「不新增第四类提示来源」、`.ts` 扫描与 `focus-ring` 两条既有缺陷的书写口径）写。上述已定而本节未明写改动的，一律继承不动。

> **一句话结论（供快读）：** 打卡视图**不是新列表、不是新区块、不是新页**——它是主列表里**每日/每周周期任务那一行的呈现变体**：完成圈即打卡（＝08 的 `completeRecurring`），行内增一枚 streak pill 与「补卡」按钮。但**「补卡」与「streak 入库」都表达不了于 08 的完成/生成模型之上**——两者都需要一份契约里还不存在的「打卡历史」数据（见第 3、8 节）。因此本规格把交互写全、把数据前置点明，**状态保持「设计」，请编排者先裁决第 8 节的契约/数据前置与实例语义分叉，再决定进「开发」**。

### 0. 现状核对（写规格前实读）

- **周期任务的完成路径（08 已落）** `src/stores/todos.ts`：`toggle` 遇到「open + 有 `recurrence` + 有 `dueDate`」走 `completeRecurring`——先问引擎下次日期，再把「完成当前实例 + 插入下一 open 实例」作为一个 `batch` 撤销单元落库（`:591-666`）。到期路径 `advanceRecurrences`（`:909`）在日切/回前台把逾期周期任务**就地前推 `dueDate`**（不建行）。**关键事实：漏掉的那一天不留任何痕迹**——没打卡时到期路径只把那一个 open 实例的 `dueDate` 往前挪，既不生成「已完成」记录、也不标「这天漏了」。
- **实例之间没有系列身份** `buildNextInstance`（`:751`）给后继 `id: newId()`、`dependsOn: []`，**不写任何指回前一实例或指向「同一个习惯」的字段**。两条实例只共享 `title`（可被编辑、可与别的任务重名）与 `recurrence`（值相同不代表同一系列）。**因此「把某个习惯的历次实例归到一起」今天做不到。**
- **周期标识 pill（08 已落）** `src/components/TodoItem.vue:246-289`：非编辑态中列的徽标行 `<p class="flex flex-wrap gap-2">`，顺序 到期 → 周期 pill（`Repeat` 图标＋`recurrenceLabel`）→ 将来。周期 pill 中性灰、非交互、不进 Tab 序；完成态褪色。颜色类串**字面写在 `.vue`**（规避 `.ts` 扫描陷阱）。
- **契约** `crates/contracts/src/lib.rs`：`Todo` 21 字段、`RecurrenceRule` 已在（`:37-116`、`:205-233`）。**没有任何「打卡历史 / 已打卡日期集合 / 连续天数 / 系列 id」字段**，`TodoPatch` 同。这是本任务与 08 的分界线——08 只需 `recurrence`（已在），本任务需要的历史数据一个都不在。
- **周期引擎** `crates/domain/src/recurrence.rs`：只有 `next_occurrence`（`:133`，从锚点严格向后取一次），**没有 `previous_occurrence` 或「区间内全部发生日」**。streak 要「从最近一次向回数连续发生日」，需要能枚举一段窗口内的排期日（见第 4、8 节）。
- **可复用组件** `src/components/ui/`：**仍只有 `button/Button.vue`**（`default` / `ghost` 两个 CVA 变体，内置 `focus-visible:focus-ring`）。没有 dialog / popover / dropdown / sheet / calendar。本任务**不新建任何基础组件**（理由同 05 第 2 节：没有导航壳与弹层地基，简化级不该顺带交付一族新组件）。
- **两条既有缺陷**（已转 `外观与设置/04`，本规格按设计意图书写、不绕开）：① `uno.config.ts` 的 `focus-ring` shortcut 实际只产出 ring 宽度；② UnoCSS 默认管线不扫 `.ts`，写在 `.ts` 的类名不生成 CSS。第 ② 条直接约束落地方式，见第 6 节。

### 1. 表态：变体，不是列表 / 不是区 / 不是页

**结论：打卡视图＝主列表里「每日/每周周期任务」那一行的呈现变体（增强版 `TodoItem`），就地渲染。不新增列表、不新增区块、不新增页、不新增路由、不新增导航。**

编排者点名要在「主列表变体」与「独立区域/页面」之间选一个，逐条给理由：

| 候选形态 | 为什么不选 |
|---------|-----------|
| **独立页 / 视图切换** | 导航壳（`桌面端形态/01`）与视图切换（`视图与统计`）都还没做（均在「拆解」态）。自建一套导航去装「打卡页」，就是造一个三个月后被 `桌面端形态/01` 整体替换的模式——与 05 第 4.1 节拒绝自建「归档页」、08 不自建详情面板是同一条理由。 |
| **独立「打卡区」（仿 05 归档区的 disclosure）** | 05 的归档区能独立成立，是因为**归档件已被 `visibleItems` 过滤出主列表**，放进区里不产生重复。习惯不同：它是**活任务**，必须留在主列表里才能被勾选完成/打卡。若再开一个「打卡区」列出同一批习惯，要么它们在页面上出现两次，要么得先把它们从主列表过滤掉——而「主列表按类型过滤」这个接缝归 `视图与统计`（05 第 4.5 节已把 `visibleItems` 留成唯一过滤口径，新增过滤维度不该由本任务私自加）。 |
| **主列表变体（选它）** | 需求原文「每日/每周周期任务**以习惯打卡形态展示**」「原独立习惯模块**并入此处**」——习惯不再是独立模块，而是周期任务的一种呈现。8/05 已经让周期任务的 open 实例就在主列表里、带周期 pill；本任务只是给「每日/每周」这一档再叠加 streak 与补卡。**可搬迁性**与 05 归档区等价：增强逻辑全部收在 `TodoItem` 的周期分支里、只读 store，导航壳/习惯视图落地那天，是「换一个容器渲染同一个增强行」，行内一行不改。 |

**「不是又一个列表」如何在结构上成立：** 本任务**不**新增任何 `v-for`、不新增任何 `<section>`、不新增任何组件文件；所有新增元素都长在 `TodoItem.vue` 非编辑态中列内部（徽标行 + 其下一行补卡按钮）。它对行的**横向宽度零增加**（沿用 08 的 `flex-wrap` 中列换行），最坏只是习惯行在窄屏多折一到两行。

**作用范围（哪些行是「习惯」）：** 仅 `recurrence.frequency ∈ {daily, weekly}` 的 open 实例。`monthly/yearly/workday/lunar` 与一次性任务**不加** streak/补卡，维持 08 的纯周期 pill——「连续几个月」不是需求要的打卡语义（需求写死「每日/每周」）。习惯身份**由频率推得，不新增 `isHabit` 开关**（少一个契约字段）。

### 2. 要求 a 的正面回答：打卡＝完成圈，不是第二个按钮

**打卡今天这一次＝行首那枚既有完成圈，点它就是 `completeRecurring`。打卡视图不再加一个和完成圈打架的「打卡」按钮。**

- 验收标准第 2 条写死「打卡即完成当日实例，与 08 实例生成一致」。完成圈今天做的正是这件事（`toggle`→`completeRecurring`：完成当前 open 实例＋生成下一实例）。**再摆一个独立「打卡」按钮＝两个控件干同一件事**，既占行宽又要向用户解释区别，是净损失。
- 因此：**完成圈就是打卡按钮**。打卡视图给它加的只有 (i) 完成时**顺带把「这一次的排期日（该 open 实例的 `dueDate`）」记进打卡历史**（这是唯一的新副作用，也是 streak 的数据来源），(ii) 行内的 streak 呈现与补卡入口。
- **完成圈的可访问名随习惯身份走**（呈现层微调，非新增提示源）：每日/每周习惯的 open 实例，圈的 `aria-label` 由「标记为完成」改读「为 {标题} 打卡」，其完成态读「撤销 {标题} 打卡」；非习惯行不变。理由：让读屏用户听到的是「打卡」而非泛泛的「完成」，语义由文字承载、不靠图标。
- **锚点口径与 08 对齐**：打卡记的是「被完成的那一次的排期日」＝该 open 实例的 `dueDate`（08 生成后继时也以 `dueDate` 为锚，见 08 实现记录第二节）。提前完成一个排期在明天的 open 实例，记的就是明天那一格——历史始终按排期日对齐，而非按「点击的墙钟时刻」。
- **撤销一致（04/08 在场）**：08 已把「完成＋插入后继」合成一个 `batch`。打卡新增的「历史写入」必须并进同一个 `batch`，使**一次 Ctrl+Z 连 streak 一起复原**（当前行回 open、后继消失、历史里那一格撤回、streak 回到打卡前）。这是 04「一次撤销＝完全复原」的既定不变量，不新增机制、只往既有 batch 里多加一条。

### 3. 要求 b 的正面回答：补卡——以及它逼出的契约/数据前置

**补卡＝把一个「过去漏掉的排期日」标记为已完成。这是本任务最难、也是最诚实的一点：08 的「完成当前实例→生成下一实例」模型里，没有任何地方能表达它。**

**为什么 08 的模型表达不了补卡（三条，都是结构性的）：**

1. **漏掉的排期日不留痕**：没打卡时，到期路径 `advanceRecurrences` 只把那一个 open 实例的 `dueDate` 往前挪，不生成任何「周一那次」的记录。等你想补周一，列表里根本没有「周一那一次」可勾。
2. **没有补进历史的操作**：`completeRecurring` 只作用于**当前 open 实例**并生成**下一次**；它无法「插入一条排期在过去、状态为已完成的实例」。硬要做，就得凭空造一条 `dueDate=周一、status=completed` 的 Todo 行——那正是「又一个列表」：每补一次卡多一条行，且补进来的行还会污染归档区与列表。
3. **实例之间没有系列身份**：就算造了那条行，它也无从归到「同一个习惯」名下（`buildNextInstance` 不写系列 id，第 0 节）。streak 要「把这个习惯的历次打卡连起来数」，靠 `title` 归组既不可靠也会串味。

**结论：补卡与 streak 都需要一份「打卡历史」——一个稳定的习惯身份 + 一组已打卡的排期日期。这两样契约里都没有，是本任务的契约/数据前置（第 8 节）。**「具体字段形态由开发定」，但 UI/UX 必须点明：**光靠 08 的完成/生成模型，补卡和 streak 无处安放。**

**补卡的交互（数据到位后即可落地，形态已定死）：**

- **补卡目标＝有界窗口内、排期日已过、且未打卡的那几天**，逐个呈现为一枚 **`补 周一` / `补 3日` 小按钮**（`Button` `ghost` 变体，`min-h-11`＝44px 触控达标，宽度随文字，见第 5 节）。窗口有界：每日取**近 7 个排期日**、每周取**近 2–3 个排期周**；并**封顶（最多 3 枚，取最近的）**——补更久以前的卡不是打卡场景（那时 streak 早断了，属回顾，归 7.4）。这条上界也是本任务与「热力图」的物理分界：本任务只给**少量、近期、可操作**的补卡入口，不铺整张日历格。
- **没有漏打时，一枚补卡按钮都不出现**（常态）。规律打卡的人看到的只有 streak pill；漏了几天的人才看到 1–3 枚补卡按钮，而那时 streak 多半已归零、pill 反而不在——两者常态上**互斥**，不会同时堆满一行。
- **补卡只写打卡历史，不建行、不碰任何 Todo 实例**（这正是「必须有历史数据结构」的原因——实例模型没处放它）。写入后 streak 依历史重算。补卡是一条独立的可撤销 `change`（撤销＝把那一格从历史移除、streak 回退），与打卡的 batch 各自独立。
- **只能补排期日**：非排期日（如「每周一三五」习惯的周日）不出现补卡按钮——排期由引擎定义。

### 4. 要求 c 的正面回答：连续记录（streak）的呈现，与热力图划界

**streak＝从「最近一个已到的排期日」向回数、连续被打卡的排期发生次数**；单位随频率：每日→天、每周→周。

- **文案**：`连续 N 天`（daily）/ `连续 N 周`（weekly），前置 lucide `Flame` 图标（描边、`:size=14`，与 08 pill 图标同档）。
- **算不算「今天还没打」**：今天（或本周）这个**当前排期尚未结束**的窗口**给宽限**——尚未打卡不算断，streak 照常显示（数到上一个已完成排期），打了卡则 +1。断裂只由**已经过去、且没补上的排期日**触发。
- **断了怎么显示**：streak 归 0。**0 时不显示 pill**（空状态＝无 pill，不摆「连续 0 天」占位）。补上最近漏的那格后，streak 依历史重算、pill 重新出现。
- **N=1 是否显示**：显示（`连续 1 天`/`连续 1 周`）——第一次打卡就给正反馈，符合习惯养成动机；只有 0 不显示。
- **数据来源**：streak 是**打卡历史（那组排期日）对排期表的纯函数派生**，不落一个会漂移的独立计数器。验收标准第 1 条「streak 数据正确入库」由此满足——**入库的是历史（原始事实），streak 是从它读出来的**，不存在「计数器与事实不符」的中间态。
- **派生放哪**：数「连续排期日」需要枚举窗口内的排期日（第 0 节：引擎今天只会向后走一次、没有 previous/区间枚举）。这属**领域逻辑，应走同一个周期引擎**（Rust/domain），不在前端另写一套日历——与 08「全应用只有一处读日历」同源，跨月/夏令时/农历边界才不会与实例生成给出两套答案。

**streak 数字/状态（本任务）与 热力图（`视图与统计 7.4`）的界——写死，供两边对照：**

| | 本任务 09（呈现） | 7.4 回顾与激励（统计） |
|---|---|---|
| 交付物 | 单个习惯行内的 **streak pill（一个数）** + 少量**近期补卡按钮** | **完成热力图**（整张日历格）、跨全部任务的**「连续完成天数」**、成就周报/徽章/分享卡 |
| 作用域 | **单个习惯**：这一个习惯连续做了 N 次 | **跨全部任务**：任意任务当天有完成、连续 N 天（与本任务的「单习惯 streak」不是一回事） |
| 可视化密度 | 一个 pill + ≤3 枚按钮，**不铺格子** | 逐日方格网格（heatmap） |
| 位置 | 主列表习惯行内 | 成就页（`7.4`，独立汇总视图） |

要点：**7.4 也列了「连续完成天数（streak）」，但那是「跨全部任务的每日完成 streak」，与本任务「某一个习惯的连续打卡」作用域不同**；本任务不做跨任务聚合、不做任何方格网格、不做徽章/分享。本任务只在**那一个习惯行**上回答「这个习惯我连着做了几次、最近漏的能不能补」。

### 5. 五项产出（组件级）

**结构与布局**

- 落点：`TodoItem.vue` 非编辑态、且 `todo.recurrence?.frequency ∈ {daily, weekly}` 的中列。**编辑态不出现任何打卡元素**（沿 01 既定：编辑态 Tab 序只有表单）。
- 新增两处，都在中列内部：
  1. **streak pill**：并入 08 既有徽标行 `<p class="flex flex-wrap gap-2">`，排在周期 pill **之后**（顺序：到期 → 周期 → **streak** → 将来）。非交互 `<span>`，仅 streak ≥ 1 时渲染。
  2. **补卡按钮行**：徽标行 `<p>` **之下**另起一个 `<div class="mt-1 flex flex-wrap gap-2">`——**按钮不能放进 `<p>`**（`<p>` 内不得含交互控件），故补卡按钮单独成行。仅当窗口内有「已过且未打」的排期日时渲染，最多 3 枚。
- Tab 序：完成圈 → 标题 → 补卡按钮（0–3）→ 复制 → 删除；DOM 顺序＝视觉顺序。
- 纯文字逻辑函数放 `src/lib/`（只产出文字、无类串）：`streakLabel(count, frequency): string`；补卡按钮日期文案（如「补 周一」）同理。**颜色/`dark:` 类串一律字面写在 `TodoItem.vue`**（第 6 节）。
- 复用图标：lucide `Flame`（streak）。**不用 emoji `🔥`**（DESIGN 禁忌「emoji 充当图标」）。dev 落地前确认 `Flame` 在 `lucide-vue-next`（同 08 对 `Repeat` 的确认口径；`Flame` 为 lucide 标准图标）。

**状态（默认/悬停/聚焦/加载/空/错误）**

| 元素 | 状态 | 表现 |
|------|------|------|
| streak pill | 默认（N≥1，open 实例） | emerald 底 pill：`inline-flex items-center gap-1 rounded-md px-1.5 py-0.5 text-xs font-medium bg-emerald-100 text-emerald-700 dark:bg-emerald-900/40 dark:text-emerald-300`＋`<Flame :size="14" aria-hidden="true" />`＋`<span class="sr-only">连续记录：</span>`＋文案 |
| streak pill | 完成态实例 | 随完成行褪色：去底、`text-slate-400 dark:text-slate-500`（照抄 08 周期 pill 完成态那对互斥类分支，不靠同权重类拼 emit 顺序） |
| streak pill | 悬停/聚焦/加载 | 非交互、不可聚焦、不进 Tab；历史随 todo 同步到达、无异步、无骨架 |
| streak pill | 空（N=0 或非习惯） | 不渲染，无占位 |
| 补卡按钮 | 默认 | `Button variant="ghost"`：`min-h-11 !px-2 !py-1 text-xs text-slate-500 dark:text-slate-400`＋文案「补 周一」 |
| 补卡按钮 | 悬停 | ghost 内置 `hover:bg-slate-100 hover:text-slate-950`（`dark:hover:text-slate-100` 显式补） |
| 补卡按钮 | 聚焦 | `focus-visible:focus-ring`（`Button.vue` CVA 内置） |
| 补卡按钮 | 点击后 | **无加载态**——写内存即返回、持久化/同步随后异步（继承 02/03/05 判断）；写入后该按钮消失（那天已补）、streak 重算、焦点归还（见可访问性） |
| 补卡按钮 | 空（无漏打） | 整行不渲染 |
| 打卡/补卡失败 | 写入 `failed`/`degraded` | 复用既有 `storageAlert → pageAlert`（05/03 排序原样生效），**不在行内另报一次** |

**暗色适配（浅 / 暗成对）**

| 元素 | 浅色 | 暗色 | 出处 |
|------|------|------|------|
| streak pill（默认） | `bg-emerald-100 text-emerald-700` | `dark:bg-emerald-900/40 dark:text-emerald-300` | DESIGN 语义色「成功用 emerald」公式；现状 `App.vue` `ALERT_TONE_CLASS.success` 同串 |
| streak pill（完成态） | `text-slate-400` | `dark:text-slate-500` | 现状完成态标题/08 周期 pill 完成态（`TodoItem.vue`） |
| `Flame` 图标 | 随文字 `currentColor` | 同 | 无需单独暗色值（08 图标同法） |
| 补卡按钮 | `text-slate-500` + ghost hover | `dark:text-slate-400` + `dark:hover:text-slate-100` | 现状次要文字与 05 复制按钮 hover 口径 |

**移动端适配**

- **触控**：streak pill 非交互，44px 下限不适用；**补卡按钮 `min-h-11`（44×44 达标）**，相邻按钮沿用 `gap-2`（8px）＝ DESIGN「相邻可点目标间隙 ≥ 8px」下限。
- **窄屏无横向滚动**：streak pill 在 `flex-wrap` 徽标行内换行；补卡按钮在独立 `flex-wrap` 行内换行。补卡封顶 3 枚、文字短（「补 周一」4 字），320px 下最多折两行，**只增高不增宽**（`documentElement.scrollWidth === clientWidth` 应在 320/375/640 成立，判据同 03/05 走查）。
- **不做滑动手势补卡/打卡**：理由同 05 第 11 节（无手势基础设施；手势对键盘/读屏不存在，须再补可见入口；移动端整体形态归 `移动端形态` 母任务）。桌面与移动是**同一套点击入口**（DESIGN 原则 2 跨端一致）。

**可访问性**

- **可访问名**（全由文字给出，不靠图标形状/颜色）：
  - 完成圈（习惯行）→ 「为 {标题} 打卡」/ 完成态「撤销 {标题} 打卡」（第 2 节）
  - streak pill → 可见文字即可访问名，前缀 `<span class="sr-only">连续记录：</span>`，读屏读出「连续记录：连续 5 天」；`Flame` `aria-hidden="true"`
  - 补卡按钮 → `aria-label="为 {标题} 补卡 {日期}"`（把「补的是哪天、哪个习惯」写进名字，不靠视觉上下文）
- **键盘全通路**：完成圈 → 标题 → 补卡（0–3）→ 复制 → 删除，DOM＝视觉。补卡点击后该按钮卸载，**焦点须显式归还**（该行的复制按钮或标题按钮，避免掉进 `<body>`——与 05 归档区还原后归还头部按钮同源；具体落点由 dev 取「同行下一个稳定可聚焦元素」）。
- **提示与播报——不新增第四类页面提示来源**（05 已确立）。打卡/补卡的回执由**结构与焦点**承担：打卡＝完成圈状态翻转（其 `aria-label` 由「打卡」翻「撤销打卡」被读出）＋ streak pill 数字变化；补卡＝该补卡按钮消失、streak pill 出现/加一。**已知取舍（明写）**：streak 数字自身变化不主动播报（不为它开第四类提示源、不重跑 05/03 优先级核对）；读屏用户在重读该行时察觉。若编排者要求可闻反馈，最小改法是复用现有单一 live region（`App.vue` 那一处），不新增源。
- **不靠颜色单传**：streak 由文字「连续 N 天」承载，emerald 只是强调；补卡由文字「补 周一」承载。
- **焦点环 / `.ts` 扫描**：按设计意图书写（`focus-visible:focus-ring`），已知 `focus-ring` 失效不绕开、不自造替代（已转 `外观与设置/04`）。
- **动效**：本任务不加入场/退场动画（列表增删动画会与撤销/重做的即时切换打架，DESIGN 要求动画不阻塞输入）。

### 6. `.ts` UnoCSS 不扫描的应对（沿 08/05）

**本任务新增的每一个颜色/`dark:` 类串都写在 `TodoItem.vue` 里，禁止写进任何 `.ts`。** `src/lib/` 只放 `streakLabel` 等**纯文字**逻辑。理由是仓库实证缺陷：UnoCSS 默认管线不扫 `.ts`，`dueDate.ts` 的 `TONE_LABEL_CLASS` 暗色一半至今未生效（03/05 已用 `pnpm run build` 产物核实）。emerald 那对 `dark:` 类串既已在 `App.vue` `ALERT_TONE_CLASS.success` 出现、会被扫描生成，本任务在 `TodoItem.vue` 再字面写一次仍是最稳的（不依赖别处恰好出现）。若日后该 emerald pill 组合多处重复，正确收敛法是 `uno.config.ts` 加 shortcut（定义在配置、使用在 `.vue`，均被正常处理）——本任务不改配置。

### 7. 令牌出处自查（每个取值可查证）

| 取值 | 出处 |
|------|------|
| `bg-emerald-100 text-emerald-700` / `dark:bg-emerald-900/40 dark:text-emerald-300` | DESIGN 语义色「成功用 emerald」公式（浅「100 底＋700 字」、暗「900/40 底＋300 字」）；现状 `App.vue` `ALERT_TONE_CLASS.success` |
| `text-slate-400` / `dark:text-slate-500`（streak 完成态） | 现状完成态标题、08 周期 pill 完成态（`TodoItem.vue`） |
| `rounded-md` / `px-1.5 py-0.5` / `gap-1` / `text-xs` / `font-medium` | 现状到期/周期/将来徽标同值；DESIGN 字号 12 辅注、字重 500 标签、间距 4 节奏 |
| `Flame :size="14"` | DESIGN 图标源 lucide、描边；`:size=14` 沿 08 pill 内联图标档 |
| `min-h-11`（44px，补卡按钮） | DESIGN「触控 ≥ 44×44」；现状 `min-h-11` 遍布 `TodoItem.vue`/`App.vue` |
| `!px-2 !py-1` / `text-slate-500 dark:text-slate-400`（补卡按钮） | 现状图标按钮 `!p-2` 同法压缩内边距；次要文字对（`App.vue`/`TodoItem.vue`） |
| `gap-2`（8px） / `mt-1`（4px） | DESIGN 间距 4/8 节奏；现状徽标行 `gap-2`、徽标行 `mt-1` |
| `focus-visible:focus-ring` | `Button.vue` CVA 内置；DESIGN 焦点基准（缺陷已转 外观与设置/04） |

**无新增设计令牌**——全部取值都能在 DESIGN.md / `uno.config.ts` / 现状代码找到出处（emerald 属既有「成功」语义档，非新造色）。

### 8. 契约/数据前置与实例语义分叉（保持「设计」，交编排者裁决）

本任务与 08 的根本差别：08 只消费既有的 `recurrence`；**本任务的补卡与 streak 需要一份契约里不存在的「打卡历史」数据**。以下三件都不是 UI/UX 能私自决定的，按全局规则 5 写在此、状态保持「设计」、停下报告编排者：

**前置一（契约/数据·必办）：打卡历史。** 至少要能表达「某个习惯 + 一组已打卡的排期日期」。具体字段形态由 dev 定，但**两块信息缺一不可**：① **稳定的习惯身份**（现状 `buildNextInstance` 每次换新 id、不留系列指针，无法把历次实例归组）；② **已打卡排期日的集合**（现状漏打不留痕、无插入历史的操作）。这是 05 加 `archived_at` 那种「dev 随实现补契约＋重生 bindings」的同类前置，但比一个可空时间戳重——所以先请编排者确认要不要做、是否因此把本子任务从「简化」上调或再拆（打卡历史落库 vs 打卡视图呈现）。

**前置二（引擎·配套）：排期日枚举。** streak 要「从最近一个排期日向回数连续打卡」，需枚举一段窗口内的排期日；引擎今天只有向后走一次的 `next_occurrence`（`crates/domain/src/recurrence.rs:133`），没有 previous/区间枚举。属 domain 侧一处薄扩展（从习惯锚点向前走、或加 previous_occurrence），随前置一一并落。

**前置三（产品/架构分叉，请产品或编排者定）：习惯的实例语义——两个自洽模型，UI/UX 推荐模型 B。**

| | **模型 A：实例串 + 历史（贴字面）** | **模型 B：单行习惯 + 历史（推荐）** |
|---|---|---|
| 打卡 | `completeRecurring`：完成当前实例、生成下一实例，**另记历史** | 只记历史 + 把这一行的 `dueDate` 推进到下次排期，**不生成「已完成」的兄弟行** |
| 与验收第 2 条 | 逐字满足「完成当日实例、与 08 实例生成一致」 | 「打卡即完成当日这一次」语义仍成立，但**不再留完成实例行**——是否算「与 08 实例生成一致」需产品/编排者定 |
| 每日习惯的列表观感 | 08 现状：每次完成留一条完成行、7 天后才归档 → **一个每日习惯最多堆 7 条褪色完成行 + 1 条 open 行**，正撞「不是又一个列表 / 保持列表轻快」 | 一个习惯**恒一行**，历史/streak 长在这行上，无堆积——真正的「习惯打卡形态」 |
| 数据落点 | 历史需系列 id 把散落实例归组（前置一①更重） | 历史天然长在那一行上，**不需要系列 id** |
| 代价 | 沿用 08 分支最省改，但观感与数据都更绕 | 需给「每日/每周习惯」在 `toggle` 里分出一条「不生成兄弟行、只推进本行 + 记历史」的路径，是对 08 完成路径的**受控改写** |

- **UI/UX 立场**：模型 B 才是「习惯打卡」，也最省数据（无系列 id、无行堆积）；但它**重新诠释了验收标准第 2 条**「与 08 实例生成一致」，所以不能由 UI/UX 单方拍板。**无论选 A 或 B，前置一的「打卡历史」都躲不掉**——分叉只影响实例是否照生。
- 请编排者/产品裁决：(1) 采纳哪个模型；(2) 若采纳 B，第 2 条验收标准是否需重述为「打卡即记录当日打卡并推进到下次排期」；(3) 是否因前置一/二把本子任务升级或拆分。**裁决前状态保持「设计」。**

### 9. 开放问题（次级，随裁决一并处理）

1. **端一致性**：`ArchiveSection.vue`（自绘历史行）与 `TodayCard.vue`（独立投影 `TodayCardTodo`，字段已裁剪）**不显示 streak/补卡**——与 08 同样把这两处留作后续端一致性补齐（08 遗留 6 已在案）。今日卡片若要显 streak，需扩同步投影载荷，超「主列表」范围。
2. **补卡窗口上界的确切值**（每日 7 天、每周 2–3 周、封顶 3 枚）是本规格给的默认；若产品对「能补多久」有别的口径，改的是常量与文案，不动结构。
3. **streak 与到期路径的交互**：模型 A 下，逾期习惯被 `advanceRecurrences` 就地推进 `dueDate`（漏打那天不打卡即算断），与 streak「过去排期日未打即断」口径一致，无冲突；模型 B 下由打卡路径自己推进，同样一致。此点两模型都成立，记录在案。

### 编排者裁决（用户 2026-07-24 拍板）

UI/UX 阶段把「习惯的实例语义」升级为需用户拍板的产品/架构分叉，编排者已就两点征询用户，结论：

1. **采纳模型 B：推进本行 + 打卡历史**（UI/UX 的推荐项）。**适用范围＝仅 `RecurrenceFrequency` 为每日 / 每周的周期任务**（＝习惯）；其余频率（每月/每年/自定义间隔/指定星期几/月末/工作日）**维持 08 现状**（完成即生成兄弟实例行）。因此完成路径按频率分流：
   - 每日/每周：打卡＝完成当日实例，但**不生成兄弟已完成行**，而是把该次排期日记进「打卡历史」并把本行推进到下一次排期。这是「打卡形态」的本义（一周不堆 7 行已完成）。
   - 其余频率：走 08 既有 `completeRecurring`，行为一字不改。
2. **本子任务不拆，整体做完**（像 05 那样：简化级但含契约字段 + schema 演进 + 引擎扩展 + 呈现交互，一轮开发⇄评审做完）。

**这重述了验收标准第 2 条**「打卡即完成当日实例，与 08 实例生成一致」——对每日/每周习惯，「与 08 一致」改为「与 08 的**推进锚点**一致，但不留兄弟已完成行、改记打卡历史」；对其余频率仍严格等同 08。

### 由此确定的三项前置（UI/UX 第 8 节所列，编排者裁决其归属）

- **打卡历史（契约·必办）**：需要「稳定的习惯身份 + 一组已打卡排期日」。字段形态由开发定（参考 03 的 `Option<Option<T>>` 与 05 的 `archived_at`，但这是一组日期而非单个时间戳）；契约变更后跑 `pnpm run types:generate` 并同步全部调用方与端侧 SQLite schema（沿用 03 的列驱动迁移）。
- **排期日反向枚举（引擎·配套）**：`crates/domain/src/recurrence.rs` 今天只有向后走一次的 `next_occurrence`；streak 要向回数连续排期日，需薄扩展（注意 08 已把候选改为增量推进的 `Cursor`，反向枚举请与之一致，不要另造一套「什么时候重复」的理解）。
- **补卡**：只写打卡历史、不建行、不改本行锚点（补的是过去的排期日，与本行「下一次」无关）。streak 从打卡历史纯函数派生，不落会漂移的计数器。

上述三项均在本子任务范围内一并完成。

## 实现记录（开发）

按裁决的**模型 B** 实现，三项前置（契约打卡历史 + 引擎反向枚举 + 补卡）一并落地，未拆分。

### 数据模型（打卡历史）

- 契约 `Todo` / `TodoPatch` 新增 `check_ins: Vec<String>`（一组已打卡排期日 `YYYY-MM-DD`），形态与 `tag_ids` 同档：`Todo` 上 `#[serde(default)]` 必填数组、`TodoPatch` 上 `Option<Vec<String>>` 整组下发（非 diff）。**习惯身份即该行 `id`**——模型 B 下习惯恒一行、永不换 id，无需系列指针（第 8 节 model B「不需要系列 id」）。校验复用 `validate_id_list`（去重 + 非空 + ≤200）。
- 存储层沿用 03 的**列驱动迁移**：`SCHEMA_VERSION` 3→4，`check_ins TEXT`（JSON）列加进 `CREATE_SCHEMA` / `ADDED_COLUMNS` / `SELECT` / `UPSERT` / `row_to_todo` / `save`，与 `tag_ids` 同法；未自造第二套迁移。历史封顶 200（`CHECK_IN_HISTORY_LIMIT`，= 契约上限），新打卡溢出时丢最旧（streak 从最新回数、补卡只看近窗，丢的是两者都不读的旧史）。

### 引擎（反向/区间枚举）

- `recurrence.rs` 新增 `occurrences_between(rule, anchor, from, to)`：**复用既有前向 `next_occurrence` 逐步喂回**（与 08 的 `Cursor` 增量推进同源，非另造一套「何时重复」），因此覆盖全部频率含农历与工作日调休；越界（07 的 `FIRST_YEAR..LAST_YEAR`）与前向单步同样报 `RecurrenceError`，不猜。反向由「从一个更早的 on-grid 排期日前向枚举」达成——每日/每周任一排期日都是合法再锚点，故 streak 用最旧打卡日、补卡用「due 回退整数个周期」得到的窗口起点做锚。
- 新增纯领域模块 `crates/domain/src/habit.rs`：`habit_progress(rule, due_date, check_ins) -> { streak, makeup }`。streak = due 之前连续被打卡的排期日数（当前 due＝下次排期，天然给「今天/本周未打不算断」的宽限，故**无需传 today**）；makeup = 近窗内已过且未打的排期日、最新在前、封顶 3。仅每日/每周（习惯）计算，其余频率返回空。纯函数、不落计数器。

### 端侧

- `src-tauri/src/lib.rs`：新增 `habit_progress` command（`HabitProgress { streak, makeup }` 线型，映射自 domain），注册进 `collect_commands!` 与 bindings 导出。
- `src/stores/todos.ts`：`check_ins` 全链路补齐（`FieldChange` / `patchFromTodo` / `add` / `duplicate` / `buildNextInstance` / `applyRemoteUpsert` 均新增，复制与生成实例都重置为空）。`toggle` 按频率分流——习惯走新 `checkInHabit`（记 due 进历史 + 引擎推进本行、**不生成兄弟行**、status 仍 open；引擎答 `null`＝系列尽头或浏览器无引擎时才真正完成），其余频率仍走 08 `completeRecurring` 一字不改。新增 `makeUp(id, date)`（只写历史、不建行、不改锚点）。打卡与补卡**各为一条 `change`**（模型 B 无兄弟行，无需 08 的 batch），一次 Ctrl+Z 连历史与 streak 一并复原（04/08 不变量保持）。`addCheckIn` 去重 + 排序 + 封顶。
- `src/lib/recurrence.ts`：新增纯文字 `isHabitRule` / `streakLabel` / `makeupShortLabel` / `isoMonthDay` 与 `habitProgress` 命令包装（非 Tauri 降级 `null`）。
- `src/components/TodoItem.vue`：非编辑态、习惯行的中列新增 ① emerald streak pill（`Flame`＋`streakLabel`，排在周期 pill 之后，streak≥1 才显、断连归 0 不显，完成态褪色）②`<p>` 之下另起补卡按钮行（≤3 枚 ghost `min-h-11`，`<p>` 内不放交互控件）。完成圈可访问名随习惯身份读「为 X 打卡 / 撤销 X 打卡」。streak/补卡候选由行内 watcher 调 `habit_progress` 缓存（依赖 rule/dueDate/checkIns 变化重算，含竞态 token）。**所有颜色/`dark:` 类串字面写在 `.vue`**（`.ts` 仅放纯文字逻辑，规避 `dueDate.ts` 扫描陷阱）。

### 改动文件

- 契约：`crates/contracts/src/lib.rs`
- 引擎/领域：`crates/domain/src/recurrence.rs`、`crates/domain/src/habit.rs`（新增）、`crates/domain/src/lib.rs`、`crates/domain/src/ics.rs`（测试构造器补字段）
- 存储/命令：`src-tauri/src/todo_db.rs`、`src-tauri/src/lib.rs`
- 服务端：`crates/server/src/sync_store.rs`（测试构造器补字段；`check_ins` 为普通 `Vec<String>`、无 flatten/带载荷变体，`patch_from_json` 回滚安全路径不受影响）
- 生成产物（`types:generate`）：`src/bindings/commands.ts`、`src/bindings/models/Todo.ts`、`src/bindings/models/TodoPatch.ts`、`src/bindings/models/HabitProgress.ts`（新增）
- 视图端：`src/stores/todos.ts`、`src/lib/recurrence.ts`、`src/lib/todo-repository.ts`（字段规则表补 `checkIns`）、`src/components/TodoItem.vue`
- 目录：`TODO.md`（1.3「打卡视图」→ ✅）

### 自验结果（命令与结论）

- `cargo test --workspace`：**224 passed**（基线 207，新增 17：habit.rs 11 + `occurrences_between` 5 + `habit_progress` 命令 1，及契约/DB round-trip 断言扩充）。覆盖 streak 断连 / 续接 / 宽限、补卡候选（含零打卡仍给近期漏打）、每日间隔与每周指定星期几、区间枚举跨月末/工作日调休/农历、07 农历越界报错、`check_ins` 经 SQLite 与 JSON round-trip 及 v1→v4 迁移读回空。
- `cargo check --workspace`：通过，无警告。
- `pnpm run types:generate`：通过，bindings 重生并已同步全部调用方（`check_ins` 入 `Todo`/`TodoPatch`，`habitProgress` 命令 + `HabitProgress` 类型）。
- `pnpm run check`：通过（格式 + lint + 类型，0 error）。
- `pnpm run build`：通过；产物 CSS 核验 emerald streak 类串（含 `dark:bg-emerald-900/40`、`text-emerald-300`）与补卡按钮 `hover:text-slate-100` 均已生成，`.ts` 未扫描陷阱不复现。
- 同步一致（另一端一致）：由服务端 `sync_store.rs` 的整份 patch round-trip 测试覆盖——`check_ins` 作为普通列表字段与 `tag_ids` 同法整组下发、原样读回。
- **走查说明（诚实标注）**：创建/设置周期规则的 UI（1.1「周期设置」）仍为 ⏳ 未开始，`TodoFields.vue` 无周期输入，故周期任务当前**无法经正常 UI 创建**——与 08 同样，习惯行为的可复现验证以上述引擎/契约/存储/同步测试为证据，未做 `tauri:dev` GUI 点选走查（无入口构造 recurrence，reviewer 亦受此约束）。未运行 app，无 appdata 残留；`dist/` 已清且被 gitignore。

### 遗留问题

- 端一致性（沿 08 遗留 6）：`ArchiveSection.vue`（自绘历史行）与 `TodayCard.vue`（`TodayCardTodo` 裁剪投影）不显示 streak/补卡；今日卡片如需显 streak 需扩同步投影载荷，超「主列表」范围。今日卡片的完成圈仍经 `store.toggle` 路由，习惯打卡照常生效。
- 系列结束的完成态 streak pill 显示为 N-1（due 停在末次排期、末次自身不计入），属罕见边界，未特殊处理。
- 补卡窗口上界（每日 8×interval 天、每周 4 周、封顶 3 枚）为规格默认值，如产品对「能补多久」另有口径，改常量即可、不动结构（第 9 节开放问题 2）。

## 评审记录

### 第 1 轮

**结论：通过，无阻塞。** 改动文件清单与工作区 `git status` 完全一致（16 改 + 2 新增：`crates/domain/src/habit.rs`、`src/bindings/models/HabitProgress.ts`），01–08 均已提交、工作区只含本任务改动，无「清单遗漏」、无无关改动。逐维核对与独立实测结果如下（正面确认从略、仅列问题条目）。

核对要点（均通过）：
- **模型 B 分流**（`src/stores/todos.ts:643-651`）：`toggle` 对 open+recurrence+dueDate 行按 `isHabitRule`（仅 daily/weekly）分流——习惯走新 `checkInHabit`（记 due 进历史 + 引擎推进本行、`status` 仍 open、不 `insertTodo`/`buildNextInstance` 生兄弟行），其余频率（monthly/yearly/workday/lunar）走 08 `completeRecurring`。核实 `completeRecurring`/`buildNextInstance` 函数体除 `buildNextInstance` 补 `checkIns: []` 外一字未改，08 相关测试（domain 129 + server 58）全过；无频率分流错。`checkInHabit` 与 `completeRecurring` 同锚（`nextOccurrence(rule, dueDate, dueDate)`）、同 `decrementedCount`，仅在引擎答 `null`（series end / 浏览器无引擎）时真正完成。
- **契约字段**：`check_ins: Vec<String>`（`Todo` `#[serde(default)]` 必填数组、`TodoPatch` `Option<Vec<String>>` 整组下发），三态由整组替换表达、形态与 `tag_ids` 同档、校验复用 `validate_id_list`；无 flatten/带载荷变体，`patch_from_json` 回滚安全路径不受影响。schema 3→4 沿用 03 列驱动迁移（`reconcile_columns` 按 `table_info` 增列、与版本号无关）。
- **反向枚举**（`recurrence.rs:173`）：`occurrences_between` 即以 `next_occurrence` 逐个答案回喂 `after` 的前向步进本身，故与逐步前进按构造等价；`from>to`→空、`after=from-1` 保含左端点、`date>to` 断出、`MAX_RANGE_OCCURRENCES` 兜底。测试覆盖每日/每周指定星期几/跨月末/跨调休(春节)/农历区间/农历越界报错/空区间/端点即发生日。
- **streak/补卡纯函数**（`habit.rs`）：streak 数 due 前（`last_due=due-1`）连续被打卡的排期日、当前 due 天然给宽限；补卡窗 `[due-window, due-1]` 只给已过、未打、近窗、封顶 3、newest-first、不给今天/未来/已打；`makeUp` 只写历史、不建行、不改锚点。均纯函数、不落计数器。

- 建议 · `crates/domain/src/habit.rs:38,111-140` · series-end 完成态 streak 显示 N-1（due 停在末次排期、末次自身作「当前 due」不计入），开发已在「遗留问题」诚实标注 · 判定为**非阻塞建议**：仅命中「有限 count/until 习惯走到系列尽头且已完成」的罕见终态，行随即归档，打卡历史（原始事实）正确入库、仅派生显示末端差一，不违背验收标准第 1 条「streak 数据正确入库」；是否修复交编排者裁决。
  - 处理（编排者）：转任务处理，并入 视图与统计/07-成就页与热力图streak。streak 的呈现与「跨任务连续完成天数」都是那个子任务的范围，series-end 的 N-1 差一属同一处派生逻辑，放它那里一次校准所有 streak 口径最省事；本任务已把打卡历史正确入库，派生显示的末端差一不影响数据正确性。
- 建议 · `src/stores/todos.ts:462` + `applyRemoteUpsert:1174` · `check_ins` 随同步整组下发 + 收端整组替换（LWW），两端各自打卡会互相覆盖丢一条 · 判定为**非阻塞建议**：已用真服务端（自起 127.0.0.1:39217、非 3000）实测——dev-A 打卡 07-20、dev-B 未见 A 时打卡 07-21，第三端拉取到两条 change 但收端 `existing.checkIns = patch.checkIns` 整组替换后仅留最后修订（07-20 丢失，不并集）；这是裁决明定的「整组下发如 tagIds」语义，非本实现缺陷。但 check_ins 的日常并发冲撞（同一每日习惯两端不同日打卡）比 tagIds 更易发生，请编排者/产品知悉——若要并集/合并语义属跨切面同步模型改动、超本简化任务范围，且在创建 UI（13）落地前不可达。单向 round-trip 一致（另一端原样收到 check_ins）、重复/空串 check_ins 经真服务端 400 拒绝、合法 200。
  - 处理（编排者）：转任务处理，并入 数据与同步/07-登录绑定同步。`check_ins` 整组替换（如 tagIds）导致两端各自打卡会互相覆盖丢失，是本任务裁决明定的语义、非实现缺陷；但正确的解是让 `check_ins` 按**集合并集**合并而非最后写入者胜——这属同步合并语义，正是 07 已并入的「字段级合并」（来自 04 评审）同一处代码同一个问题域。请 07 把「哪些字段该并集、哪些该整组替换」一并定清，不要只做标量字段的字段级合并而漏掉集合型字段。该缺口在 任务管理/13 的周期创建 UI 落地前不可达（今天无法创建周期任务，也就无法产生并发打卡）。
- 建议 · `crates/domain/src/habit.rs:99-101,117` · streak 步进锚在「全历史最旧打卡日」（`earliest_check_in`）而非近窗，理论上「200 条打卡跨 >4000 个每日排期（>11 年）且近期有短连续」的病态历史会触 `occurrences_between` 的 4000 上界、从旧端截断导致近期 streak 少算 · 判定为**非阻塞建议**（理论/不可达）：`CHECK_IN_HISTORY_LIMIT=200` + 无 11 年旧数据来源使其现实不可达，`MAX_RANGE_OCCURRENCES` 注释亦自陈「远超任何真实习惯视图」；补卡窗有界不受影响。仅作稳健性提醒，如后续收敛可改为 streak 亦从有界近窗步进。
  - 处理（编排者）：本次不处理，已知悉。评审已判定现实不可达（200 条打卡历史上限 + 无产生 11 年稀疏历史的数据源），属稳健性提醒；后续任何子任务碰到 `habit.rs` 时可顺手把步进锚点改为「区间内最近的排期日」而非全历史最旧。

关于「周期创建 UI（1.1/13）缺失是否阻断验收」：**不阻断。** 本任务验收标准（打卡/补卡可用、连续记录正确入库；打卡即完成当日实例〔模型 B 重述〕）全部落在打卡/补卡/streak 的正确性与落库上，已由 domain 单测（habit 11 + occurrences_between 5）+ 命令测 + 契约/DB round-trip + 我独立实测（真服务端同步、手工构造 v3 库升级）充分覆盖；与 08 同例（08 亦无创建 UI 而验收完成）。且浏览器 dev 运行时 `habitProgress` 因 `isTauri()` 为假降级 `null`、根本不渲染 pill/补卡，故 `pnpm run dev` 注入数据也走查不到该 UI，唯 `tauri:dev`+注入可，考量呈现层极薄且已过类型/构建校验（emerald/补卡类串经 `pnpm run build` 产物核实生成、`.ts` 扫描陷阱不复现），未做此重型走查、按 08 先例采信自动化证据。GUI 点通须待 13。

验证：cargo test --workspace 通过（224 passed：src-tauri 24 + contracts 13 + domain 129 + server 58）；cargo check --workspace 通过无警告；pnpm run check 通过（52 格式化 / 55 lint+类型 0 error）；pnpm run types:generate 通过、重生 bindings 与工作区一致无漂移；pnpm run build 通过、产物 CSS 核验 `dark:bg-emerald-900/40`(#064e3b66)、`text-emerald-300`、`dark:hover:text-slate-100`、`min-h-11` 均生成；独立实测：真服务端（127.0.0.1:39217）两端并发打卡 + round-trip + 校验拒绝，手工构造满字段 v3 库经真实迁移语句升级 v4 无损（仅增 check_ins 列、user_version 3→4、check_ins 读回 NULL→空）；测试数据已清理。
