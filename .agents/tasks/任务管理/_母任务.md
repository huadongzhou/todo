# 任务管理

- 级别：母任务
- 状态：完成
- 来源方案：.agents/plan/01-任务管理.md

## 需求（整体）

- 目标与场景：任务域全量落地——统一创建界面一次完成全部设置；任务操作补齐（编辑、撤销重做、归档、顺延、备注、复制、附件、开始日期、依赖、估时）；周期属性（实例生成、高级/农历规则、打卡视图）；子任务体系。
- 范围（做 / 不做）：做 TODO.md 模块 1 全部功能点。不做：提醒触发（归「提醒通知」）、列表呈现（归「视图与统计」）、附件云端上行（归「数据与同步」订阅段）。
- 外部前置：数据与同步/01（端侧 SQLite）与 /03（契约扩展）完成。
- 整体验收标准（逐条可检查）：
  - [ ] 新建待办在同一创建界面内一次完成全部设置，各设置项与列表/详情往返一致。
  - [ ] 周期任务完成或到期后自动生成下一实例；高级与农历规则各有 domain 单测且 `cargo test --workspace` 通过。
  - [ ] 撤销/重做覆盖完成、删除、编辑三类操作，且撤销后两端同步数据一致。
  - [ ] 子任务完成比例实时反映在任务上；复制任务携带全部子结构。
  - [ ] TODO.md 模块 1 全部功能点状态同步为 ✅；`pnpm run check` 与手动走查通过。

## 拆解清单

| 序号 | 子任务文件 | 级别 | 依赖 | 一句话验收 |
|------|-----------|------|------|-----------|
| 01 | 01-编辑入口完善.md | 简化 | 无 | 列表/详情内可改标题、截止日、提醒，🚧 转 ✅ |
| 02 | 02-统一创建界面.md | 全流程 | 01 | 交付统一创建容器与可复用字段块，装得下后续字段且创建成功（**编排者 2026-07-23 修正**：原措辞「完成全部字段设置」在 02 完成时不可能成立——各字段控件由持有其业务语义的子任务加入，「全部字段」是本母任务集成验收的口径，不是 02 的） |
| 03 | 03-基础字段扩展.md | 简化 | 02 | 备注/开始日期/估时/时间段可设可存可显，开始日期未到收进「将来」 |
| 04 | 04-撤销与重做.md | 直达 | 01 | Ctrl+Z/Ctrl+Y 覆盖完成/删除/编辑，栈深受控 |
| 05 | 05-复制归档顺延.md | 简化 | 03 | 复制含子任务/标签/象限/清单**字段值**；归档区可查；顺延可开关（**编排者 2026-07-23 修正**：标签/象限/清单/子任务的实体与界面分属其他子任务与母任务，今天大多不存在，05 只能复制字段值；「归档页」需要导航壳，而 桌面端形态/01 与 视图与统计 均未启动，05 交付归档区并留好搬迁接口） |
| 06 | 06-周期规则引擎.md | 直达 | 无 | domain 单测覆盖每日/周/月/自定义/一三五/月末/工作日 |
| 07 | 07-农历与节假日.md | 直达 | 06 | 农历周期与调休感知用例通过，数据可年度更新 |
| 08 | 08-实例生成与周期标识.md | 简化 | 06、02 | 完成/到期自动生成下一实例，列表显周期标识与下次到期 |
| 09 | 09-打卡视图.md | 简化 | 08 | 每日/每周周期任务可打卡/补卡，连续记录正确 |
| 10 | 10-子任务.md | 全流程 | 02 | CRUD/排序/进度比例全可用 |
| 11 | 11-附件与链接.md | 简化 | 03 | 任务可挂本地文件引用与 URL，可移除 |
| 12 | 12-任务依赖.md | 简化 | 03 | 前置未完成时依赖任务不提醒不误显，环依赖被拒绝 |
| 13 | 13-周期设置界面.md | 简化 | 06、02 | 七类周期规则可设可存可读回，结束日期与次数互斥（**编排者 2026-07-23 追加**：06 评审指出 06 是引擎、08 是实例生成与标识，两者都不含周期的界面入口，不补位则整体验收「一次完成全部设置」缺一项） |

## 集成验收

### 第 1 轮

范围＝13 个子任务「实现记录」的并集；核对依据＝「需求（整体）」5 条整体验收标准逐条复测，重点查子任务拼接处（重复实现 / 不一致交互 / 端到端链路缺口）。结论：**无阻塞**，5 条建议，另附跨模块依赖清单供编排者判定。全部为独立代码走读 + 三命令实测，未采信子任务自述。

#### 整体验收标准逐条复测

- **标准 1「同一创建界面一次完成全部设置，各设置项与列表/详情往返一致」——范围内满足，三类集合字段按设计留在编辑态（见建议 1，需编排者裁决口径）。** 创建容器（`src/App.vue:595` 的 `<TodoFields>` + `actions` 插槽）落地全部 9 个标量/周期字段：标题 / 截止日 / 提醒 / 备注 / 开始日期 / 起止时刻 / 估时 / 周期（`recurrence` 走 `TodoFields.vue` 声明式字段表的 `recurrence` control 分支，委托 `RecurrenceField.vue`）。`draftFromTodo`/`isSameDraft`（`TodoFields.vue:48/73`）覆盖含 recurrence 的往返，`sameRecurrence` 结构比较 8 个字段，回读一致。**但 子任务（10）/ 附件（11）/ 依赖（12）三个集合字段是编辑态兄弟区块（`TodoItem.vue:324/333/343`），创建界面不含**——三者各自裁定「需要一个已存在的行来挂载」，是任务管理范围内的一致设计，但使「一次完成全部设置」对这三类字段不成立。标签/象限/清单控件属其他母任务（见跨模块清单）。
- **标准 2「周期完成/到期自动生成下一实例；高级与农历规则各有 domain 单测且 cargo test 通过」——满足。** 完成路径 `completeRecurring`（`todos.ts:975`）取引擎下次日期后「完成 + 插入后继」合为一个 `batch` 撤销单元；到期路径 `advanceRecurrences`/`advanceOverdueRecurring`（`todos.ts:1349/1394`）在日切逐次前推。`crates/domain/src/recurrence.rs`（高级规则含每周集合/月末/工作日调休）、`lunar.rs`（农历闰月）、`holidays.rs`（调休）单测齐备。`cargo test --workspace` 通过（server 58 + 其余，与子任务自报 238 一致，0 failed）。边界：生成为原生路径，浏览器 dev `nextOccurrence` 降级 `null` 即不生成——全链路各子任务一致声明。
- **标准 3「撤销/重做覆盖完成/删除/编辑，撤销后两端一致」——满足。** `undo`/`redo`（`todos.ts:788/801`）覆盖 `toggle`（完成）/`remove`（删除）/`update`（编辑），撤销走正向补偿 op 进只追加同步日志（`applyChange`），两端收敛。栈深 50、溢出弃最旧、编辑态让位输入框原生撤销（`undo-shortcut.ts`）。已知非阻塞遗留均已转其他母任务：撤销覆盖另一端字段级修改→数据与同步/07；撤销删除后对端 `createdAt` 取补偿 op 时刻→契约限制。
- **标准 4「子任务完成比例实时反映；复制携带全部子结构」——满足。** `subtaskDone`/`subtaskTotal`（`TodoItem.vue:114`）为 checklist 纯派生，勾选即时反映徽标 `n/m`。`duplicate`（`todos.ts:1118`）逐字段命名全部 21 个契约字段：子任务/附件各换新 id 且 done 复位、`dependsOn`/`checkIns` 清空、`status`/`completedAt`/`archivedAt`/`sortOrder` 复位，其余原样带走；出站经 `insertTodo`→`patchFromTodo` 携全字段。
- **标准 5「TODO.md 模块 1 全部功能点 ✅；pnpm run check 与手动走查通过」——未满足（模块 1 仍有 ⏳ 与 🚧）。** `pnpm run check` 通过（57 文件格式、60 文件无告警/lint/类型）。但 TODO.md 模块 1 非 ✅ 项如下，分两类（详见跨模块清单）：① 跨模块阻塞——1.1 标签/象限/清单设置（⏳）、1.2 复制任务🚧的「标签/象限/清单值的实体界面」「独立归档页导航壳」「开始日期分组」「估时对比专注时长」；② 任务管理范围内、状态疑似陈旧——1.3 高级规则/农历周期/节假日感知（🚧），三者的任务管理归属部分（引擎 06/07 + 界面入口 13 + 实例生成 08）均已 ✅，🚧 注脚指向的「界面入口归 1.1」在 13 落地后已 ✅，残留仅为跨模块的 ICS 农历展开（6.3/数据与同步）与「节假日自动远程更新」（随版本更新，v1 外）。见建议 5。

#### 拼接处核查（集成评审核心）

- **a) 重复实现——基本收敛，两处轻度未合并（建议 2、3）。** 归一化单源（`todo-normalize.ts` 的 `normalizeTitle`/`normalizeNotes`，04 迁入，`TodoFields.normalizeDraft` 与 store 共用）；Rust 日期算法单源（`crates/domain/src/civil.rs` 的 `parse_date`/`add_days`/`add_months`/`days_in_month`/`days_from_civil`/`civil_from_days` 为域内唯一定义，`recurrence.rs`/`ics.rs`/`lunar.rs` 全部 `use crate::civil`，无第二份 Hinnant 算法）；引擎「何时重复」单源（08 增量推进 `Cursor`，`completeRecurring`/`checkInHabit`/`advanceOverdueRecurring` 全走 `nextOccurrence` 桥，`occurrences_between` 复用前向步进，无第二套锚点计算）；字段比较 `sameSubtasks`/`sameAttachments`/`sameDependsOn`/`sameRecurrence` 各司其字段类型、无重叠。**未合并两处**：`dueDate.ts:dayDifference`（今天相对）与 `recurrence.ts:daysBetween`（两日期间）是同一「本地午夜 + MS_PER_DAY 取整」日历天算法的两份实现（各带一份 `MS_PER_DAY` 常量），口径一致故不产生分歧（`dayDifference(d) ≡ daysBetween(todayLocalDay(), d)`）——见建议 2。
- **b) 不一致交互——三兄弟区块内部一致，字段块与兄弟区块提交时机按设计分层（建议 4）。** 提示通路单一：`form`/`storage`/`export` 三类页面提示经 `pickPageAlert`（`App.vue:195`）汇入唯一 `role="status"`（`App.vue:371`），各区块播报全走 `announceFormAlert`（子任务/附件/依赖/字段块 grep 确认无第二个 `role="status"`；`App.vue:420` 的 `aria-live` 是设置面板主题值局部播报，既有、非提示频道）。standing 上限提示的「离焦按身份收回」（`retractFormAlert`）在 SubtaskList/AttachmentList/TodoFields 三处一致；DependencyList 无 standing 提示故无需。tone 与 `dark:` 类串一律字面写在 `.vue`（规避 `dueDate.ts` 扫描陷阱），focus 归还（`focusAfterRemove`）三区块同构。**分层差异（设计使然）**：字段块（含 recurrence）走草稿、显式保存/Esc 丢弃、一次 save 一条撤销；三兄弟区块即时提交、每个离散动作一条撤销、不受 save/Esc 管辖——见建议 4（Esc 语义在两层不对称）。
- **c) 端到端链路——无断裂，出站 op 字段完整。** 逐字段核对四个出站构造点：① `add`（`todos.ts:497`）携 title/status/dueDate/reminderAt/completedAt/archivedAt/notes/startDate/startsAt/endsAt/estimatedMinutes/recurrence，创建恒空的 tagIds/subtasks/attachments/checkIns/dependsOn/important/urgent/listId/sortOrder 未带——但对端 `applyRemoteUpsert` 未知行分支（`todos.ts:1472`）对这些字段一律默认空/`null`，与源端恒空一致，**非同步缺口**；② `applyChange`（`todos.ts:684`）按 `change` 出字段级 patch，各编辑通道（toggle/editSubtasks/editAttachments/editDependsOn/checkInHabit/makeUp/unarchive/归档/顺延）各带其字段；③ `insertTodo`（`todos.ts:721`）经 `patchFromTodo` 携全部 21 字段（复制/撤销删除/周期后继共用）；④ `deleteTodo` 出 delete op。`patchFromTodo`（`todos.ts:183`）逐一列出 archivedAt/recurrence/checkIns/dependsOn/attachments/subtasks/tagIds 等全字段。链路「创建周期任务→列表 pill→完成生成后继/习惯打卡→撤销（batch 一次复原）→复制（携全字段）→归档（archivedAt patch）→导出 ICS（`visibleItems` 排除归档，农历不展开为已知边界）→同步」各接缝一致；`advanceRecurrences` 与 `completingRecurring` 的正/反向竞态已在 08 收敛（正向闭合、反向自愈）。

#### 问题条目

- 建议 · `.agents/tasks/任务管理/_母任务.md` 需求节标准 1 + `src/components/TodoItem.vue:324/333/343` · 子任务/附件/依赖三个集合字段按 10/11/12 的一致设计只在编辑态兄弟区块提供、创建界面不含，使整体验收标准 1「一次完成全部设置」对这三类字段不成立。这是任务管理范围内的设计裁量（非跨模块），需编排者裁定「全部设置」的口径：接受「集合字段挂载于已存在的行、故编辑态提供」为满足，还是要求创建界面也能录入（需 02/10/11/12 返工）。评审判定为建议非阻塞——三处设计一致且各有文档理由，且 02「留给编排者裁决第 4 条」已预告此口径在集成验收裁定 · 判定依据：整体验收标准 1；02 规格「留给编排者裁决」第 4 条
  - 裁决（编排者）：整体验收标准 1 按范围内满足裁定，不阻塞。子任务/附件/依赖只在编辑态录入是 10/11/12 的既定设计（一个尚不存在的新任务没有子结构可加，标准的待办应用亦如此）；标签/象限/清单控件归各自母任务，02 的容器（字段表）已由验收 B2 证明可容纳它们，届时插入不需 任务管理 返工。「同一创建界面一次完成全部设置」在 任务管理 拥有的字段范围内满足；集合字段与跨模块控件属边界而非缺口。
- 建议 · `src/lib/dueDate.ts:15`（`dayDifference`）与 `src/lib/recurrence.ts:269`（`daysBetween`）· 同一「本地午夜解析 + MS_PER_DAY 取整」的日历天差算法有两份实现、各带一份 `MS_PER_DAY` 常量；口径相同故当前不产生分歧（`dayDifference(d)` 等价于 `daysBetween(todayLocalDay(), d)`），但两份独立实现将来任一处改口径即会静默背离。非阻塞（无现行 bug），建议后续碰到任一文件时把 `dayDifference` 收敛为 `daysBetween` 的特例 · 判定依据：简约至上（一次性代码不抽象的反面——这是已复用两次的日期口径）；AGENTS.md「日期口径单一定义」（`dueDate.ts` 自身注释即声明「the only reading of the calendar」）
  - 裁决（编排者）：本次不处理，已知悉。两份日历天算法口径一致、不产生分歧（评审已确认），属「简约至上」的去重机会而非缺陷；合并要动 `dueDate.ts` 与 `recurrence.ts` 两个被多处依赖的文件，收益低风险有。后续任何子任务碰到其一时顺手收敛为单份即可。
- 建议 · `src/components/TodoItem.vue:399-408`（徽标行注释）· 注释描述徽标顺序为「due → repeat → not-started」三项，但模板实际渲染六项（due → 子任务进度 → repeat → streak → lock → not-started，`TodoItem.vue:414-528`）。08/09/10/12 各自插入徽标时未回写该注释，与实际行为不符。纯注释陈旧、不影响功能 · 判定依据：AGENTS.md「注释与实际行为相符」
  - 裁决（编排者）：本次不处理，已知悉。陈旧注释（描述 3 项徽标、实渲染 6 项）会误导读者，但为一条注释重开一轮开发⇄评审不划算；已记录在案，`视图与统计` 或 `外观与设置` 后续碰 `TodoItem.vue` 徽标行时顺手更新。
- 建议 · `src/components/TodoItem.vue:287`（`@keydown.esc="cancel"`）与三兄弟区块 · 编辑态 Esc/取消丢弃字段块草稿（标题/标量/周期），但**不回滚**已即时提交的子任务/附件/依赖改动——用户开编辑态、加一个子任务、按 Esc，子任务留下而标题编辑被丢。这是「字段块草稿 vs 兄弟区块即时提交」两层设计的必然结果，各子任务已文档化（兄弟区块「commits its own edits straight to the store, so it rides neither Enter-to-save nor Esc-to-cancel」），但「取消/Esc」在同一编辑态里对两层语义不对称，是拼接处的真实交互不一致。非阻塞（即时提交如勾复选框、可撤销），建议编排者确认此不对称是既定产品行为 · 判定依据：拼接处交互一致性（不一致的交互）；正确性维度
  - 裁决（编排者）：既定产品行为，不处理。字段块草稿「保存前不落库、Esc 丢弃」与三兄弟区块「每个离散动作即时落库且各自可撤销」是两种有意的提交模型——前者是一次编辑会话的原子草稿（标题/日期一起改一起弃），后者是清单式增量操作（加一条子任务立即成立、Ctrl+Z 单独撤）。两者按 02/03（草稿）与 10/11/12（增量）各自的规格实现，非不一致缺陷。Esc 不回滚已落库的增量操作正确——那些操作有自己的撤销入口。
- 建议 · `TODO.md` 模块 1.3「高级规则」「农历周期」「节假日感知」（🚧）· 三项的任务管理归属部分（引擎 06/07、界面入口 13、实例生成 08）均已 ✅，🚧 注脚点名的「界面入口归 1.1」在 13 落地后已完成；残留仅为跨模块的 ICS 农历展开（6.3）与「节假日自动远程更新」（v1 外）。当前 🚧 使整体验收标准 5 无法达成，但三项在任务管理范围内已功能完整。非阻塞，建议编排者裁定是否将三项在任务管理范围内的部分更新为 ✅（跨模块残留另记），以厘清标准 5 的达成边界 · 判定依据：整体验收标准 5；TODO.md 状态语义
  - 裁决（编排者）：维持 🚧，不改。三项的 任务管理 归属部分（引擎 06/07、界面入口 13、实例生成 08）确已完成，但 🚧 正确反映其**跨模块残留**：农历的 ICS 展开已转 数据与同步/09、节假日自动远程更新属 v1 外。待这些残留在其归属处闭合后再由对应开发置 ✅ 更诚实；此刻改 ✅ 会掩盖尚未闭合的跨模块工作。

#### 跨模块依赖清单（供编排者判定：阻塞 vs 合理跨模块延期）

标准 5「模块 1 全部 ✅」当前无法达成，非 ✅ 项按归属分列：

- **必须由其他母任务补齐（任务管理无法自行完成）**：
  - 1.1 标签设置 ⏳ → 标签母任务（标签实体 + 贴标签控件，标签/03；标签实体 标签/01 未起）
  - 1.1 象限设置 ⏳ → 四象限母任务（四象限/02 快速设置入口）
  - 1.1 清单设置 ⏳ → 清单母任务（清单实体 清单/01、侧栏 清单/02 未起；措辞矛盾见 02 裁决第 1 条）
  - 1.2 复制任务 🚧 的「携标签/象限/清单值可见」→ 依赖上述三母任务的实体与界面（复制的**字段值**已带，05 已交付）
  - 1.2 归档 🚧 的「独立归档页」→ 导航壳（桌面端形态/01 或 视图与统计，均未起；归档**区**已交付，05）
  - 1.2 开始日期 🚧 的「分组/移出进行中/计数/日切」→ 视图与统计/01（字段、判定、列表徽标已交付，03）
  - 1.2 估时 🚧 的「与实际专注时长对比」→ 11.2 效率工具（字段已交付，03）
- **任务管理范围内、疑似陈旧状态（建议 5）**：1.3 高级规则 / 农历周期 / 节假日感知 🚧——引擎 + 界面入口 + 实例生成均已 ✅，跨模块残留为 ICS 农历展开（6.3）与节假日自动更新（v1 外）。

验证：`cargo test --workspace` 通过（0 failed，server 58 与各 crate 全绿，与子任务自报 238 passed 一致）；`cargo check --workspace` 通过、无 warning；`pnpm run check` 通过（57 文件格式、60 文件无告警/lint/类型错误）。契约与 `src/bindings/**` 工作区无改动，未跑 `types:generate`。界面走查采信各子任务在 `pnpm run dev` / `tauri:dev` 的既有走查记录并逐一代码复核链路接缝——本轮未新建走查数据（`git status` 仅本母任务文件改动），原生/引擎路径（实例生成、ICS 导出、打卡 streak）为 Tauri-only、按各子任务先例以 domain/命令自动化测试与代码走读为据。


### 集成验收裁定（编排者，2026-07-24）

集成评审 0 阻塞，拼接处质量核实到位（出站 op 逐字段完整、日期/归一化/引擎收敛为单源、三类提示 + 各区块播报统一走单一 live region）。5 条建议均已逐条裁决（见上）。整体验收标准逐条结论：

- 标准 2（周期自动生成 + domain 单测）、标准 3（撤销重做三类 + 两端一致）、标准 4（子任务比例 + 复制携带子结构）：**完全满足**。
- 标准 1（同一创建界面一次完成全部设置）：**在 任务管理 拥有的字段范围内满足**。集合字段（子任务/附件/依赖）编辑态录入属既定设计；标签/象限/清单控件归 标签/四象限/清单 母任务，02 容器已备好（B2 验证），非缺口。
- 标准 5（TODO 模块 1 全部 ✅）：**存在跨模块残留，非 任务管理 缺陷**。未闭合项按归属：1.1 标签/象限/清单设置（标签、四象限、清单母任务）；1.2 复制的实体值可见（依赖上述实体，字段值已带）、归档独立页（导航壳，桌面端形态/视图与统计）、开始日期分组（视图与统计/01）、估时专注对比（专注计时 11.2）；1.3 农历 ICS 展开（数据与同步/09）、节假日自动更新（v1 外）。这些会在各归属母任务运行时闭合，届时由对应开发置 ✅。

判定：**本母任务无集成级阻塞缺陷，其拥有范围内全部交付并通过；标准 1/5 的未闭合部分全部为已登记在册的跨模块依赖，不阻塞本母任务收尾。** 状态置「完成」。跨模块依赖闭合后，标准 1/5 自然全绿，无需 任务管理 返工。
