# 实例生成与周期标识

- 级别：简化
- 状态：设计

## 需求（产品）

- 目标与场景：带周期的任务完成或到期后自动生成下一实例（Rust 侧执行）；列表可见周期标识与下次到期。
- 范围（做 / 不做）：做 实例生成落库与标识展示；不做 提醒触发（提醒通知母任务）。
- 验收标准（逐条可检查）：
  - [ ] 完成/到期两条路径均自动生成下一实例，规则来自 06/07 引擎。
  - [ ] 列表显示周期标识与下次到期；同步后另一端一致。

## 规格（UI/UX）

## 实现记录（开发）

- 改动文件：
- 自验结果（命令与结论）：

## 评审记录
