# 迷你捕捉窗

- 级别：全流程
- 状态：需求

## 需求（产品）

<!-- 编排者预填草案：产品阶段修订确认后再进设计 -->

- 目标与场景：全局快捷键弹出独立小输入窗秒记待办后即关，不唤起主窗、不打断当前工作；内置一键贴入剪贴板文本（点击时才读取、不监听）。
- 范围（做 / 不做）：做 迷你窗、全局键绑定、剪贴板按钮；不做 自然语言解析。
- 验收标准（逐条可检查）：
  - [ ] 任意应用前台按全局键弹窗、录入回车即存即关；焦点还给原应用。
  - [ ] 剪贴板仅在点击按钮时读取；pnpm run check 与走查通过。

## 规格（UI/UX）

## 实现记录（开发）

- 改动文件：
- 自验结果（命令与结论）：

## 评审记录
