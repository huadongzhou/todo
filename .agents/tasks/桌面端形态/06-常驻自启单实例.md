# 常驻自启单实例

- 级别：直达
- 状态：开发

## 需求（产品）

- 目标与场景：月历表嵌入桌面层级、应用开机自启（可关）、单实例（二次启动聚焦已有实例）。
- 范围（做 / 不做）：做 三能力；单实例先验证 tauri-plugin-single-instance 就绪度，不可用则命名互斥量自实现。
- 验收标准（逐条可检查）：
  - [ ] Windows 10/11 实机各验证一次；嵌入层级失败降级为置顶窗口并 log::warn 不崩溃。
  - [ ] 自启开关生效；二次启动聚焦并转发参数（深链预留）。

## 规格（UI/UX）

## 实现记录（开发）

- 改动文件：
- 自验结果（命令与结论）：

## 评审记录
