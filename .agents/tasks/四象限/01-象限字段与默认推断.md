# 象限字段与默认推断

- 级别：直达
- 状态：开发

## 需求（产品）

- 目标与场景：重要 × 紧急标记字段落契约；默认推断规则（截止日临近预填紧急）落 domain。
- 范围（做 / 不做）：做 字段与推断规则（临近阈值为常量）；不做 界面（02/03）。
- 验收标准（逐条可检查）：
  - [ ] 字段进契约且 bindings 同步；推断规则 domain 单测通过。
  - [ ] 推断只预填、不覆盖用户显式标记。

## 规格（UI/UX）

## 实现记录（开发）

- 改动文件：
- 自验结果（命令与结论）：

## 评审记录
