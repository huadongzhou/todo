# 排序与拖拽

- 级别：简化
- 状态：设计

## 需求（产品）

- 目标与场景：象限内排序 + 跨象限拖拽（拖动即改重要×紧急标记）。
- 范围（做 / 不做）：做 拖拽交互（复用 7.1 手动排序字段，不另造）；不做 触屏适配（移动端阶段）。
- 验收标准（逐条可检查）：
  - [ ] 跨象限拖拽即改标记；排序持久化，刷新/重启/同步后不回跳。

## 规格（UI/UX）

## 实现记录（开发）

- 改动文件：
- 自验结果（命令与结论）：

## 评审记录
