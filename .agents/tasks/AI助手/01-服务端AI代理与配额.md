# 服务端AI代理与配额

- 级别：直达
- 状态：开发

## 需求（产品）

- 目标与场景：LLM 调用统一走服务端代理（key 不出服务端），按账号配额计量，全部 AI 能力的底座。（外部前置：账号与登录/01–02 完成）
- 范围（做 / 不做）：做 代理 service、配额模型（额度常量集中配置）、客户端调用通道（沿 sync-transport 模式）；不做 各能力本体（02–08）。
- 验收标准（逐条可检查）：
  - [ ] 代理通道可用；API key 仅存服务端环境配置。
  - [ ] 配额按账号计量准确（并发不重复扣/漏扣），超额拦截且提示友好；有 cargo test。

## 规格（UI/UX）

## 实现记录（开发）

- 改动文件：
- 自验结果（命令与结论）：

## 评审记录
