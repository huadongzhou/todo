# 用户表与注册登录API

- 级别：直达
- 状态：开发

## 需求（产品）

- 目标与场景：服务端用户体系——用户表、注册/登录/登出接口、token 签发与验证（邮箱+密码自建 token，已锁定）。
- 范围（做 / 不做）：做 用户表、/v1/auth/* 接口、密码哈希（安全依赖选型理由写实现记录）；不做 客户端界面（03）。
- 验收标准（逐条可检查）：
  - [ ] 注册/登录/登出接口与 token 签发验证有 cargo test 覆盖（含错误路径）。
  - [ ] 密码非明文存储；契约 DTO 派生规范符合 AGENTS.md。

## 规格（UI/UX）

## 实现记录（开发）

- 改动文件：
- 自验结果（命令与结论）：

## 评审记录
