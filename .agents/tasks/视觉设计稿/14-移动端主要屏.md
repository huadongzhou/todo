# 移动端主要屏

- 级别：简化
- 状态：设计

## 需求（产品）

- 目标与场景：设计移动端主要屏，复用桌面设计的移动适配。
- 范围（做 / 不做）：做 移动主屏（底部三段导航 + 列表）、移动创建界面、我的页、滑动手势态（左滑完成/右滑删除等）、触控与 safe-area 适配、主屏小组件；375/768 断点。；不做 产品代码改动（只写 .agents/design/<本子任务子目录>/）、不做「复现」（按图实现归对应功能模块）、不新增设计令牌（DESIGN.md 缺口停下报告编排者）。
- 覆盖模块：移动端形态（全）
- 验收标准（逐条可检查）：
  - [ ] 移动布局复用桌面字段/待办行视觉的移动适配，正文 ≥16px 防 iOS 缩放、无横向滚动、触控 ≥44px、safe-area 避让
  - [ ] 滑动手势与主屏小组件形态齐备
  - [ ] 导出移动浅/暗 PNG（及必要平板分栏）到 .agents/design/14-移动端主要屏/

## 规格（UI/UX）

<!-- UI/UX 阶段：以 DESIGN.md 令牌 + 该屏已有文字 UI/UX 规格（若模块已编码）/现状代码为输入，给出本屏的构图、状态清单、浅暗与移动适配、可访问性；不新增令牌，缺口停下报告。 -->

## 实现记录（开发）

<!-- 开发阶段：产出自包含 HTML 视觉稿（内联 DESIGN.md 令牌值），Browser 预览渲染后截图导出 light.png/dark.png（移动端屏另出移动版）到 .agents/design/<子目录>/；HTML 源一并留存。 -->

- 改动文件：
- 自验结果（命令与结论）：

## 评审记录
