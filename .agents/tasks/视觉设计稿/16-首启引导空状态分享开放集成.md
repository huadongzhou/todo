# 首启引导空状态分享开放集成

- 级别：简化
- 状态：设计

## 需求（产品）

- 目标与场景：设计首启引导、全局空状态、分享卡片与开放集成设置界面。
- 范围（做 / 不做）：做 首启引导（欢迎/空状态引导）、各视图空状态汇总、分享卡片导出、开放集成设置（API Token 管理、Webhook、网页只读分享、URI 深链）；含首次与常态。；不做 产品代码改动（只写 .agents/design/<本子任务子目录>/）、不做「复现」（按图实现归对应功能模块）、不新增设计令牌（DESIGN.md 缺口停下报告编排者）。
- 覆盖模块：发布工程（首启/引导/空态）、开放与集成、视图与统计/09
- 验收标准（逐条可检查）：
  - [ ] 首启引导低摩擦、空态有下一步引导、分享卡片可导出形态清晰
  - [ ] 开放集成设置（Token/Webhook/只读分享）符合设置面板与表单基准
  - [ ] 导出浅/暗 PNG 到 .agents/design/16-首启引导空状态分享开放集成/

## 规格（UI/UX）

<!-- UI/UX 阶段：以 DESIGN.md 令牌 + 该屏已有文字 UI/UX 规格（若模块已编码）/现状代码为输入，给出本屏的构图、状态清单、浅暗与移动适配、可访问性；不新增令牌，缺口停下报告。 -->

## 实现记录（开发）

<!-- 开发阶段：产出自包含 HTML 视觉稿（内联 DESIGN.md 令牌值），Browser 预览渲染后截图导出 light.png/dark.png（移动端屏另出移动版）到 .agents/design/<子目录>/；HTML 源一并留存。 -->

- 改动文件：
- 自验结果（命令与结论）：

## 评审记录
