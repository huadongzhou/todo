# 统一创建与编辑界面

- 级别：简化
- 状态：设计

## 需求（产品）

- 目标与场景：设计统一创建表单与行内编辑态，字段块全字段展开。
- 范围（做 / 不做）：做 创建表单（首行标题 + 详情区）与 TodoItem 行内编辑态两种容器形态；字段块全展开：标题（含码点计数器）/截止日/多提醒点/周期规则（渐进式）/标签/象限/清单/备注 textarea/开始日期/时间段/估时/子任务清单/附件/依赖；字段级校验错误与提示条（三类提示优先级）态。；不做 产品代码改动（只写 .agents/design/<本子任务子目录>/）、不做「复现」（按图实现归对应功能模块）、不新增设计令牌（DESIGN.md 缺口停下报告编排者）。
- 覆盖模块：任务管理/02/03/13、标签（贴标签）、四象限（设置）、清单（归属）、提醒（多点）
- 验收标准（逐条可检查）：
  - [ ] 字段块按分组呈现（内容/时间/归类/关联），全字段有可见 label、blur 校验、语义 type
  - [ ] 创建与编辑两容器复用同一字段块视觉；提示走单一提示条形态
  - [ ] 导出浅/暗 PNG 到 .agents/design/03-统一创建与编辑界面/

## 规格（UI/UX）

<!-- UI/UX 阶段：以 DESIGN.md 令牌 + 该屏已有文字 UI/UX 规格（若模块已编码）/现状代码为输入，给出本屏的构图、状态清单、浅暗与移动适配、可访问性；不新增令牌，缺口停下报告。 -->

## 实现记录（开发）

<!-- 开发阶段：产出自包含 HTML 视觉稿（内联 DESIGN.md 令牌值），Browser 预览渲染后截图导出 light.png/dark.png（移动端屏另出移动版）到 .agents/design/<子目录>/；HTML 源一并留存。 -->

- 改动文件：
- 自验结果（命令与结论）：

## 评审记录
