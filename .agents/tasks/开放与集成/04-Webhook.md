# Webhook

- 级别：直达
- 状态：开发

## 需求（产品）

- 目标与场景：任务变更出站推送——签名头、失败重试上限、可停用。
- 范围（做 / 不做）：做 订阅注册、推送、签名与重试；不做 事件类型的无限扩展（首版任务变更）。
- 验收标准（逐条可检查）：
  - [ ] 变更触发推送且签名可验；失败重试有上限并可查；停用即停。

## 规格（UI/UX）

## 实现记录（开发）

- 改动文件：
- 自验结果（命令与结论）：

## 评审记录
