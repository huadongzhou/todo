# 触控与safe-area适配

- 级别：简化
- 状态：设计

## 需求（产品）

- 目标与场景：触控目标 ≥44px、safe-area 避让、基础手势区（DESIGN.md 基准）。
- 范围（做 / 不做）：做 全局适配审查与修正；不做 滑动手势（05）。
- 验收标准（逐条可检查）：
  - [ ] 抽查主要界面触控目标达标；刘海/手势区不遮挡内容；无文字溢出。

### 编排者并入项（来自 任务管理/01 的 UI/UX 阶段发现）

**既有待办行的触控区低于 DESIGN.md 的 44px 基准**：完成圈约 24px、删除按钮约 33px（`src/App.vue` 待办行，任务管理/01 会把这部分抽进 `src/components/TodoItem.vue`）。与编辑入口无因果关系，属既有清账项，归本子任务统一处理。

补充验收标准：

- [ ] 待办行内全部可点元素（完成圈、删除、编辑入口）的触控区达到 DESIGN.md 的 ≥44px 基准。
- [ ] 子任务清单的上/下移按钮触控区达 ≥44×44px（来自 任务管理/10 评审第 1 轮：实测 44×30，是规格指定的唯一触屏排序通道）；若与行密度冲突，给出统一取舍策略。

## 规格（UI/UX）

## 实现记录（开发）

- 改动文件：
- 自验结果（命令与结论）：

## 评审记录
