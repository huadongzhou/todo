# 跨端待办应用 · 设计系统

样式编码所用设计令牌以本文档为唯一事实源；工程与目录规范见 AGENTS.md。所有视觉决策面向桌面与移动两种形态，浅色与暗色成对设计。

标注说明：「现状」= 已在代码中生效的事实；「基准」= 本文档新确立的约定，后续编码必须遵循。

## 设计原则

1. **内容优先、低摩擦**：待办的录入与完成是核心动线，界面为内容让路；单屏只有一个主操作（Primary CTA）。
2. **跨端一致，平台各敬**：同一套令牌与组件跨 Windows/macOS/Linux/iOS/Android 复用；触控尺寸、键盘操作、系统手势遵循各平台惯例。
3. **浅暗成对**：任何新样式必须同时给出浅色与暗色（`dark:` 变体），暗色是降饱和的调亮/调暗变体，不是简单反色；两种模式的对比度分别验证。
4. **可访问优先**：正文对比 ≥ 4.5:1、焦点可见、触控目标 ≥ 44px、信息不单靠颜色传达。
5. **克制动效**：微交互 150–300ms，只动 `transform`/`opacity`，尊重 `prefers-reduced-motion`。

## 设计风格

主风格：**极简扁平（Minimalism × Flat Design）· 微阴影变体**。

- **视觉特征**：纯色块、无渐变、2D 平面感；排版层级清晰、留白充足；内容优先；图标统一为描边风格（lucide）。
- **与纯扁平的既定偏差（现状）**：保留唯一一档 `shadow-sm` 微阴影（`surface-card`）用于卡片与页面的层级区分；除此之外层级一律靠边框与背景差表达。
- **布局模式**：极简单列（Minimal Single Column）——单栏内容流、移动优先、单屏单一主操作；当前主界面与今日卡片均属此模式，新页面默认沿用。
- **交互质感**：微交互层——hover/press 以颜色或透明度位移表达（如按钮 sky-500 → sky-600），时长与缓动遵循「动效」节；不做大幅位移或装饰性动画。
- **风格禁忌**：渐变、玻璃拟态（装饰性 `backdrop-blur`）、新拟态、多重或彩色阴影、3D 立体效果、同层级混用填充/描边图标、emoji 充当图标。超出本节边界的视觉尝试须先修订本节再落码。

## 设计令牌

### 品牌色（现状）

| 令牌         | 值                   | 出处与用法                                       |
| ------------ | -------------------- | ------------------------------------------------ |
| `brand`      | `#0ea5e9`（sky-500） | `uno.config.ts` theme.colors；主按钮底色、焦点环 |
| brand 交互态 | sky-600 `#0284c7`    | hover 加深（Button.vue `hover:bg-sky-600`）      |

### 中性色（现状：slate 阶）

| 用途         | 浅色                | 暗色                |
| ------------ | ------------------- | ------------------- |
| 页面背景     | slate-50 `#f8fafc`  | slate-950 `#020617` |
| 卡片表面     | white               | slate-950           |
| 正文         | slate-900 `#0f172a` | slate-200 `#e2e8f0` |
| 次要文字     | slate-600           | slate-300/400       |
| 边框与分隔线 | slate-200           | slate-800           |

来源：`src/styles/main.css` 与 `uno.config.ts` 的 `surface-card`。新增灰阶取值一律使用 slate，不混入 gray/zinc/neutral。

### 语义色（现状：到期状态三档，`src/lib/dueDate.ts`）

| 语义                 | 浅色                          | 暗色                                       |
| -------------------- | ----------------------------- | ------------------------------------------ |
| overdue（逾期/危险） | `bg-red-100 text-red-700`     | `dark:bg-red-900/40 dark:text-red-300`     |
| soon（临近/警示）    | `bg-amber-100 text-amber-700` | `dark:bg-amber-900/40 dark:text-amber-300` |
| normal（常规）       | `bg-slate-100 text-slate-600` | `dark:bg-slate-800 dark:text-slate-300`    |

基准：新的语义场景（成功、错误提示等）沿用同一公式——浅色「100 底 + 700 字」、暗色「900/40 底 + 300 字」；成功色用 emerald。语义色必须伴随文字或图标，禁止只靠颜色区分状态。任何语义 chip 都必须成对给出 `dark:` 变体（现状 `ReminderSnoozeBar.vue` 头部 info chip 缺暗色对，属需修订的现状偏差，复现/维护时补 `dark:bg-sky-900/40 dark:text-sky-300`）。

顺序色阶（基准，设计稿 08 确立，用户 2026-07-25 裁决同步）：贡献热力图用 emerald 单色 4 档——浅色 `emerald-100 / 300 / 500 / 700`、暗色 `emerald-900 / 700 / 500 / 300`；空档复用中性 `slate-100`（暗 `slate-800`），端点复用 success 令牌。属 emerald hue 顺序延伸、非平行新色；须带 `role="img"` + 图例（少→多）+ 逐格数值，颜色不孤用。

AI 视觉身份（基准，用户 2026-07-25 裁决）：AI 界面（拆解/周报/建议卡/配额条）复用 **info(sky)** 语义着色，**不引入 AI 专属强调色**；AI 的辨识度靠图标（sparkles/crown）与「AI」文字标签而非颜色。日后若要给 AI 独立视觉身份，属新增令牌决策，须另行修订本文档。

### 暗色模式机制（现状）

- 偏好三档：`light` / `dark` / `system`（`src/lib/appearance.ts`）。
- `applyTheme` 同时写 `data-theme` 属性（驱动 `main.css`）与 `.dark` 类（驱动 UnoCSS `dark:` 变体），Tauri 桌面端同步原生窗口主题。
- 编码规则：样式一律用 `dark:` 变体成对提供；禁止针对单一模式硬编码颜色后不给另一模式对应值。

### 字体

- 字族（现状）：`Inter, ui-sans-serif, system-ui, …`（`main.css`），无衬线单字族，不引入第二字族。
- 字号阶梯（基准）：`12 / 14 / 16 / 18 / 24 / 32`——12 辅注、14 次要正文与按钮、16 正文基准、18 强调、24 区块标题、32 页面标题。移动端正文不小于 16px（避免 iOS 聚焦自动缩放）。
- 大数字 / display 档（基准，设计稿 09/10 确立，用户 2026-07-25 裁决采纳）：`48 / 60 / 72`，仅用于番茄钟、禅模式等英雄计时数字（Inter + `tabular-nums`）；不用于正文或标题，字族/字重/对齐不越界。
- 行高（基准）：正文 1.5–1.75，标题 1.2–1.3。
- 字重（基准）：正文 400、标签 500、标题 600–700。
- 数字（基准）：计数、日期等对齐场景用 `tabular-nums` 防跳动。

### 间距与布局（基准）

- 间距走 4/8 节奏；区块层级用 16 / 24 / 32 / 48。
- 触控目标 ≥ 44×44px，相邻可点目标间隙 ≥ 8px；命中区不足时扩展 padding 而非放大图标。
- 最小视口 320px（现状 `body min-width`）；移动优先，断点基准 375 / 768 / 1024 / 1440。
- 禁止移动端横向滚动；固定顶栏/底栏需为内容预留内边距，移动端优先 `min-h-dvh` 而非 `100vh`。

### 圆角与阴影（现状 + 基准）

- 卡片：`surface-card` shortcut（`rounded-xl` + 1px 边框 + `shadow-sm`），阴影只此一档，不自造阴影值。
- 按钮与输入：`rounded-lg`。
- 层级靠边框与背景差表达，不靠加重阴影；暗色模式下阴影弱化、以边框区分层级。

### 动效（基准）

- 微交互 150–300ms；进场 `ease-out`、退场 `ease-in`，退场时长约为进场的 60–70%。
- 只动 `transform` 与 `opacity`，禁止动画 width/height/top/left 引发重排。
- 列表项入场可 30–50ms 逐项错峰；动画不得阻塞输入、必须可中断。
- 尊重 `prefers-reduced-motion`：降级为直接状态切换。

### 图标（现状 + 基准）

- 唯一图标源：`lucide-vue-next`（SVG 描边风格），同一层级不混用填充/描边两种风格；禁止 emoji 充当图标。
- 尺寸档（基准）：16 内联 / 20 按钮 / 24 独立操作。
- 纯图标按钮必须带 `aria-label`，命中区补足至 44px。

## 组件视觉基准

- **卡片**：一律复用 `surface-card`；卡片内边距 16 或 24，不逐处自定义。
- **按钮**：变体集中在 `src/components/ui/button/Button.vue`（CVA）——`default`（brand 底白字）与 `ghost`（slate 文字 + hover 浅底）；禁用态 `opacity-50` + 禁指针；新变体加进 CVA 定义，不在调用处散写类串。（`ghost` 暗色文字用 slate-300 与浅色成对——设计稿 01 确立，补记现状 `Button.vue` ghost 缺的 `dark:` 变体。）`destructive` 变体（破坏性 CTA，设计稿 12 确立、用户 2026-07-25 裁决同步）：red-600 `#dc2626` 底 / hover red-700 `#b91c1c` / 白字，白字压 red-600 对比 ≈5.9:1 达标；危险区（danger zone）边框用 `--danger-border` red-200 `#fecaca`（暗 red-900·60 `rgba(127,29,29,0.6)`）——均为 red 语义 hue 延伸、非平行新色，落码时补进 CVA。
- **焦点**：任何可交互元素聚焦时显示 `focus-ring` shortcut（2px sky-500 环 + 2px offset，暗色 offset 对应 slate-950）；禁止移除焦点样式。
- **表单**：输入框必须有可见 label（AGENTS.md HTML 规范）；错误信息紧贴对应字段下方并说明修复方式（两种输入形态的错误态：有边框白底输入用 red-500/暗 red-400 红边框——设计稿 01 确立；填充式输入 `border-0 bg-slate-100` 现状用红字 `text-red-600`/暗 red-400 + `aria-invalid` 不改边框——设计稿 03 核实现状）；失焦（blur）时校验而非逐键校验；移动端输入高度 ≥ 44px；语义化 `type`（date/time 等）唤起正确键盘。
- **待办行**：完成态用降低文字对比 + 删除线表达，同时保留可读性；操作按钮（完成/删除）触控区 ≥ 44px；删除等破坏性操作用 red 语义色并与常规操作在视觉上分离。
- **徽标与标签（两族，勿混——集成评审 2026-07-25 确立）**：① **行内到期徽标**（逾期/今天/N 天后，`dueDate.ts` 语义三档）= `rounded-md`(6px) 圆角矩形 + padding `2px 6px` + 12px，对齐现状 `TodoItem.vue`/`TodayCard.vue` 与全部功能屏；② **状态/分类标签 chip**（标签、角色、共享态、清单归属等）= 胶囊 `9999px`。两族圆角不同、按用途取，复现时勿把行内到期徽标做成胶囊。标签分类色见「待补充」（当前语义五色 -500 占位）。
- **移动端滑动操作条（基准，设计稿 14 确立、用户 2026-07-25 裁决同步）**：待办行左滑完成 / 右滑删除的动作条用语义 700 实心底 + 白字——完成 `emerald-700 #047857`（白字 ≈5.5:1）、删除 `red-600 #dc2626`（白字 ≈4.8:1）；实心底不随浅暗翻转（平台惯例），均为既有令牌 hex 的新用法、非新色；动作附图标 + 文字，不孤靠颜色。
- **空状态**：给出说明文案 + 下一步引导（如「还没有待办，回车快速添加」），不留白屏；加载超过 300ms 显示骨架或进度提示。
- **弹层（设置面板/今日卡片等）**：提供明确关闭途径（按钮 + Esc）；移动端支持下滑关闭；遮罩 40–60% 黑并在暗色下同样保证前景隔离。

## 可访问性基准

- 正文对比 ≥ 4.5:1，大字号与图形元素 ≥ 3:1；浅暗两种模式分别验证。
- 键盘全通路：Tab 顺序与视觉顺序一致，弹层内焦点圈闭、关闭后归还焦点。
- 状态信息（逾期、错误、成功）必须有文字或图标伴随，色彩不孤用（现状：到期标签已带文字 ✓）。
- **已知偏差**：`default` 按钮白字压 sky-500 对比约 2.6:1，未达 AA 正文标准。待定决策：底色加深至 sky-600 或改用深色文字；调整属视觉变更，走轻量流程并同步本文档。

## 待补充

- 品牌延展：Logo、App 图标、插画风格未定义（用户 2026-07-25 裁决保持待补充，随「发布工程」模块落地时定义；设计稿 16 暂用「Just Do」字标 + sky 勾选占位）。
- 移动端 safe-area 细则（刘海、手势条避让）：设计稿 14 以 `env(safe-area-inset-*)` + 顶 44 / 底 34 示意占位；正式数值待 iOS/Android 构建落地按实机补充。
- 标签分类调色板：现设计稿用语义五色 -500 档（sky/emerald/amber/red/slate）占位；最终档数、命名、暗色策略归「标签」模块编码时确定并补入本文档（用户 2026-07-25 裁决：归模块定，当前不锁死）。
- 外观与设置相关规格（用户 2026-07-25 裁决保持待补充，随「外观与设置」模块实现时定义）：高对比调色板、字号缩放档位与作用范围、完成动效与音效规格、快捷键默认键位表。
- 组件清单随实现扩充：checkbox、dialog、toast（系统通知，设计稿 11 以中性卡示意）、AI 建议卡（info 顶条 + 采纳/编辑/丢弃，设计稿 15）、分享卡与 mono 代码块（设计稿 16）、订阅墙/配额条（brand CTA + info + crown，设计稿 15）等新组件落地时在「组件视觉基准」增补对应条目。
