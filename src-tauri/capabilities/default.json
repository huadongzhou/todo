{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Todo application.",
  "windows": ["main"],
  "permissions": ["core:default", "log:default"]
}
